# ═══════════════════════════════════════════════════════════════════════════
# ARENCON PROTECTED SYMBOLS — features Mark specified. THE GATE WILL NOT LET
# THESE DIE. --kill does not work on them. The ONLY way to remove one is Mark
# editing this file himself.
#
# WHY (S493): the 1000-series band grouping Mark specified in June 2026 was
# silently removed by a later session — likely a revert/cleanup that could not
# tell "feature Mark asked for" from "leftover experiment" — and had to be
# recovered from past-session search. Several features have died this way.
# Handoffs go stale; this file lives in the repo, so every session hits it.
#
# FORMAT: one symbol per line. A line starting with @ names the feature the
# following symbols belong to (shown in the block message so the session knows
# WHAT it was about to destroy). Blank lines and # comments ignored.
#
# DISCIPLINE: every new Mark-specified feature gets its load-bearing symbols
# added here AS PART OF SHIPPING — same as the cache bump. Unlisted = unprotected.
# ═══════════════════════════════════════════════════════════════════════════

@ 1000-series band grouping on the Hub dashboard (server filing order; Mark, Jun 2026, restored S493)
_bandLo
_bandLabel
_renderBanded
toggleBand
.band-header
.band-section
.band-range
.band-count
.project-grid-inner.is-banded

@ Export Project Docs — whole-project bundle from Hub detail, dashboard, and card (one export, one meaning; S488-S493)
_epdRun
_epdPickProject
exportProjectDocsFromDashboard
exportSingleProject
.epd-pick
.epd-pick-search
.epd-pick-row
.epd-pick-band

@ Backup stamping — "backed up" means a real bundle was taken (S493)
_stampExported

@ Project notes never lost to the save debounce (S493)
_notesPending
_debounceSaveNotes
_saveNotes

@ Sign-in boot path guards — a missing element must not blank the Hub (S493)
_showLoginScreen
showApp
setupKeyboardShortcuts
tryRestoreSession

@ Purge type-to-confirm — permanent delete can never be one mistap (S119)
_typeToConfirmPurge
purgeProject

@ Photo-loss protections in the shared export reader (S493 — Electric/DD photos were silently dropped)
srcOf
photoIdentityKey
bucketFromRaw
commissioningPhotos
ddPhotos

@ Photo data-integrity core — audited delete, pointer protection, tombstones (S481/S393; catastrophic if lost)
delPhotoGuarded
_protectPhotoPointer
_makePurgedTombstone
mint

@ Lightbox selection engine — pick/group/unlink canon, locked S344 (unlink is never delete)
confirmPick
ungroupActive
groupActiveId
pickCount
isPicking
_groupBounds

@ Shared photo input — Camera + Upload + Gallery standard, one implementation (S478/S479)
PhotoInput

@ Shared dialog engine — all ten families; every tool's modals route here
typeToConfirm
confirmDanger
leave
# 'form' removed from this list by Mark's explicit order, Session 590 (03 Aug 2026,
# in-conversation): the cross-device conflict modal ("This report was changed by
# someone else") is DELETED from Diesel and Electric — conflicts resolve
# automatically, newest ENTRY wins via the S583 entry-time stamps; the badge
# notification system replaces the modal as the review surface. The dialog
# engine itself and its form family remain live and untouched in
# lib/ui/dialogEngine.js and its other call sites.
pickList
progress

@ Whole-project bundle builder (shared lib; Hub + tools consume it)
buildAndDownload
defaultReadme

@ Hub edit-role via shared form family; edit-project unsaved-changes guard (S493, Mark's say-so)
openEditRole
_epSnapshot
_epReadFields
@ Seal redaction — PDF appendix covers (S492, Mark-specified; LOCKED_SEAL_REDACTION.md; decided WARN)
_drawSealCovers
_drawSealCoversCropped
_openRedactionEditor
SEAL_COVER_LABEL
@ Seal redaction visibility — markers/badges/contact sheet (S492, Mark-approved demo; LOCKED_SEAL_REDACTION_VISIBILITY.md)
_renderSealMarkersInViewer
_sealUndo
_sealSnapshot
_toggleSealEditMode
_sealApplyAll
_frtRefreshSealMarkers
_sealRows

@ Cards/List view toggle — locked June list view, recovered from pre-wipe blob 4e8837a1 and restored (S494, Mark: "This is it. Wire it")
setHubView
_hubView
.hub-view-toggle
.view-list

@ Tool filter chips — filter the board by which tool a project uses (S494, Mark-specified; search cannot do this)
setToolFilter
_toolFilter
.tool-chip

@ Admin roster fields — User #, Initials, Edit Name, Reset Password (Mark-specified June; deleted by S391 stale push; restored verbatim S494 from pre-wipe blob 4e8837a1)
openEditNum
saveUserNum
openEditInit
saveUserInit
openEditName
saveUserName
openResetPw
submitResetPw
genTempPassword

@ Modal safety — backdrop clicks never close modals; every modal carries an X routed through its own Cancel path (Mark, S494)
_injectModalX
.modal-x

@ Tools & Reports hierarchy — activation demoted to a slim settings strip, reports promoted to accent-bar rows, "+ New Report" small and quiet (never burgundy, never large — rejected twice) (Mark, approved S494, shipped S495 Lane B)
.tool-strip
.ts-lbl
.tw
.tw-sw
.rgroup
.rghead
.rg-ic
.rg-name
.rg-cnt
.btn-new
.rrow
.rmain
.rtitle
.rmeta
.rctl
.rempty

@ Deleted Reports Stage 1 — report delete is a SOFT delete (deleted_at/deleted_by), restorable from the project; deleted reports filtered at the SOURCE so they never leak into exports; permanent delete is admin-only (Mark, design S494, shipped S495 Lane B)
_loadAndRenderDeletedInstances
restoreInstance
purgeInstance
_canPurgeReports
detail-deleted-section
detail-deleted-instances
deleted-reports-count
.rrow.is-deleted

@ New Project client suggestion by number series + duplicate project-number warning — suggests clients already used under that series (canonical spelling ranks first), warns on a duplicate full number without blocking, PM defaults to creator (Mark, requested S489, shipped S495 Lane B)
_npSeriesOf
_npClientsForSeries
_npOnNumberInput
_npPickClient
np-client-suggest
np-dupe-warn
.np-suggest
.np-suggest-chip
.np-note-warn

@ Backup discipline — one-tap select of every never-backed-up project, and backup AGE tiers (30/60/90/never) as TEXT TINT ONLY, never card fills or left edges (Mark, roadmap, shipped S495 Lane B)
selectNotBackedUp
.backup-status.age-30
.backup-status.age-60
.backup-status.age-90


@ HOST CONTRACT — Diesel: functions the SHARED /lib/ modules call BY NAME (Claude, S496, after the S492 dead-code sweep deleted five of these and made the Diesel/Electric checklist photo zone inert over live data). These are NOT a Mark-specified feature — they are the wiring that makes the shared engines work. They are listed here because the failure mode is EXACTLY what this manifest exists to stop: a session greps the tool, sees zero callers, declares the function dead, and --kill lets it through. `removePhoto` is the proof — grep Diesel and it has ZERO callers, yet lib/ui/checklist.js calls it from a generated onclick. node --check cannot see late-bound globals; only a lib grep can. Before deleting ANY function from a tool that consumes /lib/, grep EVERY lib module.
_aConfirm
_collectCloudState
_dslLoadBakeImage
_dslMarkupPersist
_dslMarkupRevert
_dslRefreshPhotoSurfaces
_galleryReuseChecklist
_isPhotoDeleted
_isRealImageBlob
_phSrc
_photoSrc
_r2EnqueuePhoto
_r2Fname
_rebuildMkDisplay
_renderPhotoGallery
debounceAutosave
handleDrop
openCameraBurst
openLightbox
openLightboxMarkup
removePhoto
saveState
setStatus
showToast
toggleItemDetail
triggerPhoto
updateCompletionOverview
updateProgress
updateVerdict

@ HOST CONTRACT — Electric: callbacks the sealed header engine v2 and the shared photo input invoke by name via deferred window lookups (Claude, S496). Same reasoning as the Diesel block above: these are called from lib/ui/headerConfigs.js and lib/ui/photoInput.js, not from Electric itself, so a reference scan of Electric alone shows them as unused. Deleting any of them silently breaks the header controls or the photo zone.
handleFiles
handleFlowTestDrop
handleFlowTestDropPld
issueReport
goBackToHub
exportPDF
downloadJSON
importJSON
_r2ReuploadAll
_r2CleanupOrphans
resetCurrentPage
resetAllPages
_openToolQR
toggleDarkMode
cycleTextSize
signOutSession
updateDarkToggleIcon

@ EXPORT QUALITY — FRT (Mark-specified, S496). Per-export photo/drawing quality tiers plus PER-SHEET drawing DPI overrides, replacing the flat 5 MP drawing cap that rendered every sheet at ~63 DPI regardless of size. Mark specified 175 DPI as the baseline and specifically required per-sheet control ("some dwg doesnt need high dpi"). _qDrawingMaxPx also carries the device memory budget that stops a large sheet crashing an export on a field tablet — removing it reintroduces a crash-on-site risk, not just a quality regression. The seal-tile slider markup (.exq-tile / data-exq-sl) is the ONLY surface for the per-sheet control; deleting it strands the feature with no UI.
PDF_TIERS
PDF_SHEET_IN
_qTierDef
_qPeakMB
_qBudgetMB
_qDrawingMaxPx
_applyExportQuality
_getQualityNotes
_qDwgTierDef
@ Hidden-report badge — a tool switched OFF that still holds reports shows a count badge; work is never silently hidden. enabled_tools filters the SCREEN ONLY, never exports (Mark, S495)
_toolReportCounts
.tw-hid
.tw.has-hidden

@ Export must contain report data — _fetchProjectToolData reads the array execute() returns; a failed read must NOT stamp the project as backed up (S495 bug fix)
_fetchProjectToolData

@ Backup honesty — an export containing zero reports downloads but is NOT stamped as a backup, and read failures are reported; a stamp on a hollow file is a false all-clear (S495, root cause of the S127/S495 hollow-export class)
_exportHasReportData

@ Canonical tool order — report groups, dashboard tool pills and Deleted Reports all follow AVAILABLE_TOOLS (FRT, DFP, EFP, IST, OBC, DD), never enabled_tools storage order (Mark, S495)
_orderTools

@ Retention Stage 2 — 90-day countdown chips on deleted reports and trashed projects; nightly reporter tools/retention_sweep.mjs has NO delete verb (reports only, anomaly cap goes red); purge audit records the purger via the existing DELETE snapshot trigger (Mark, design S494, shipped S495 Lane B)
RETENTION_DAYS
RETENTION_WARN_DAYS
_retentionOf
.ret-chip
.ret-warn
.ret-expired

@ One tab per report — clicking a report already open FOCUSES its tab instead of opening a second one; two tabs on one report both autosave to the same row and the loser's work is lost silently. Middle/ctrl/right-click still opens a deliberate second copy (Mark, original design restored S495)
arencon_rpt_

@ Range bands always visible — band/tool chips count the WHOLE scope (not just unpinned leftovers), so My Projects shows its ranges; counts stay stable while a filter is active, and an ACTIVE filter narrows pinned/starred too (Mark, S495)
_chipBase
_chipScope

@ PIN REMOVED — star is the only project override. Pin was GLOBAL (one person's pin sat on everyone's board); star is per-user. Mark: explicit and final, S495. Do NOT reintroduce a global pin; if firm-wide attention is ever needed that is a separate announcement feature, not a sort order. is_pinned remains in the DB, unread.
starred-section
toggleStar
selectAllStarred

@ sw.js CACHE_NAME is a UTC TIMESTAMP, never a counter — a counter let two lanes silently revert each other's bump (near-miss S495, both picked v1174). Bump = set to current UTC yyyymmddhhmm. Do NOT reintroduce an incrementing version (Mark, S495)
CACHE_NAME

@ Print-size photo scaling — photos embed at their PRINTED 3-across cell size (photoDpi × PDF_PHOTO_CELL_IN), tier photoPx is only a ceiling (Mark's queue item 5, S497). Do not revert to flat long-edge targets.
PDF_PHOTO_CELL_IN
@ Insights panel — backup coverage, backup age and active-projects-per-person, computed from the loaded board with hand-drawn SVG donuts, no chart library (Mark, roadmap item, shipped S497 Lane B; re-appended 2x after shared-file contention dropped this block — manifest needs a mechanical merge fix)
openInsights
_donutSvg

@ What's New + Hub Guide — Help panel fed by updates.json, which is edited ONLY when Mark says so, never automatically; amber unseen-update dot per device (Mark, roadmap item, shipped S497 Lane B)
openHelp
_wnCheck
.wn-dot

@ Duplicate project indicator — later-created projects sharing a number carry an amber Duplicate chip; the first-created never does; trashed projects neither carry nor cause it (Mark, field test S497b)
_isDupProject
.badge-dup

@ Global client autocomplete in New Project — typing matches every client on record, most-used spelling first, offered never forced (Mark, field test S497b)
_npOnClientInput
_npAllClients
@ Portal shared header — index.html header is the sealed engine + lib/ui/portalHeaderConfig.js; theme on the shared device key so the portal matches every other tool (Mark, field test S497c). The hand-rolled portal header/toggle is retired and must not return.
portalHeaderConfig
.imx-head
.imx-body
@ Duplicate project numbers BLOCKED at creation (Mark, explicit S497d; supersedes S495 warn-only) + admin Client Name Cleanup + R2 Reclamation Stage 3 UI (Mark present and instructing; deletes ONLY the nightly report's still-existing candidates via the authenticated worker)
renderClientCleanup
ccMergeCluster
ccMergeManual
_ccNorm
renderReclamation
rrDelete
@ Client-name guards for everyone, not admin-only (Mark, S497e): create-time snap to existing spelling on case/punctuation match; edit-time ripple offering to propagate a corrected client name to every project spelling it the old way
_clientCanonicalFor
# ── S498 Lane B (Mark): client rename across projects + correction requests ──
openClientRename
_crnApply
_crnOnName
_notifInit
_notifRenderCard
_notifAccept
_notifAcceptAll
_notifDeclineRow
_notifReview
crn-open
notif-stack
client-rename-modal
notif-review-modal

@ Flow-point gauge/RPM photo modal — extracted to lib/ui/flowPhotoModal.js (Mark, S500; With/Without-PLD modes, reading tags, reassign, gallery reuse)
openFlowPhotos
closeFlowPhotos
_renderFlowPhotoModal
_flowPhotoAddFile
_flowPhotoIcon
_flowPhotoOpenGallery
_flowPhotoOpenReassign
_flowPhotoSetActiveMode
_GAUGE_READINGS
_GAUGE_TAG_LABEL

@ Deficiencies panel — extracted to lib/ui/deficiencies.js (Mark, S500; contractor groups, general defic, priorities, IAR, findings roll-up, All-view + CSV)
renderDeficGroups
buildDeficItem
renderGeneralDeficGroup
buildGeneralDeficItem
updateDeficSummary
_renderChecklistFindings
deficiencies
generalDeficiencies
deficPhotoTarget

@ CRB unlock / amend / revert / history — printed comments editable via deliberate unlock with a forever audit log (Mark, S500; overrides locked §1 freeze-all)
unlockThreadEntry
revertThreadEntry
_logAmend
_threadEntryAmendable
crbt-unlock
crbt-revert
crbt-history
amendedAfterIssue

@ CRB import-batch undo — pull a whole contractor-PDF import back out in one tap; hard-removes untouched rows, preserves rows you've reviewed (Mark, S500-C)
undoImportBatch
_noticeUndo

@ CRB report-side amendment printing — an amended-after-issue comment prints its original issued wording in the next report, so the paper record isn't silently changed (Mark, S500-D)
_crbAmendNote
crb-amend

@ Diesel deficiency status pill — FRT S263 port (Mark-locked, S502; one tappable pill, 5-state popover, rec/site classification flags, data mapping)
_deficStatusPill
_deficPickStatus
_deficChoice
_deficEnsureMenu
_deficTogglePillMenu
_CV_CHOICES
isRecommendation
isSiteRecord

@ CRB stale-sheet acknowledgement — a contractor PDF answering an older report than the project's current round no longer hard-blocks; warns and files on the current round by default, backfill-anyway available (Mark, S500-E)
_confirmStale
_runImport

@ Diesel Help & guide — shared searchable Help engine mounted in Diesel's sealed dialog (ArenconDlg.panel), Diesel's own cards in lib/ui/dieselHelpCards.js, "?" button folds with the icon trio. openHelp/_helpSetDot live in BOTH twins (monolith + diesel-app beta) and are invoked by name from the shared header config's onHelp — a grep of Diesel alone shows the header wiring as the only caller. Coming-soon fallback if cards fail to register. (Mark, S505, help/guide roadmap seed)
openHelp
_helpSetDot

@ FRT Help & guide — shared searchable Help engine in the shared sealed dialog, FRT's own cards in lib/ui/frtHelpCards.js, scope 'FRT' (one panel = one scope, never mixed). openHelp/_helpSetDot are invoked by name from the shared header config's onHelp, so a grep of app.js shows the header wiring as the only caller. Coming-soon fallback if cards fail to register. (Mark, S505g)
openHelp
_helpSetDot

@ CRB re-import diff — a re-sent contractor sheet is reconciled instead of silently dropped. Deliberate removal of an IMPORTED comment writes a tombstone (wording included); a re-import then offers it back one-tap, and declining records the declined wording so an identical re-send stays quiet while a changed one re-surfaces. Reworded answers split on issued state: an unissued draft offers Replace/Keep-both, an ALREADY ISSUED answer is never overwritten and lands as a new round. findLiveResponseByDedupe MUST walk contractors[].deficiencies AND generalDeficiencies — there is no flat _project.deficiencies. _writeRow is the single writer for every branch; do not add a second one. (Mark, S508)
_ensureCommentTombs
_normTombText
_writeCommentTomb
findCommentTomb
dismissCommentTomb
clearCommentTomb
tombDecisionFor
findLiveResponseByDedupe
replaceUnissuedImportedDraft
_classifyDupes
_resolveDiffQueue
_confirmOfferBack
_confirmReworded
_writeRow
_diffOverlay
_quoteBlock
_sizeChartsForPrint
_dslVerdict
_dslVerdictFacts

@ Help system S509-S510 (Mark) — task-style guides + contextual "?" across the platform. Dashboard (23 cards) and Hub page (13) rebuilt with real-screen replica art and Mark's invented cast — NO real staff/client/project data may return to any guide drawing. Diesel rebuilt task-style (15 cards); Electric wired for the first time (8 cards, verified surfaces only — expand with the photo-architecture port, never ahead of it). Existing card ids, search-term strings and dates are carried VERBATIM on any future rebuild. In-place "?" buttons: Hub New Project + Insights, FRT drawing-viewer toolbar (#dv-help-btn) and pin editor (.dfx-ed-help) via the window._frtHelpAt bridge in frt/js/app.js. tools/check_help_art.py must pass (0 collisions) after touching any guide drawing.
_frtHelpAt
.dfx-ed-help
ART.hpPage
electricHelpCards

@ Issue soft-lock lifecycle + working-copy imports + DRAFT watermark. Issuing is asked BEFORE export generation; a working copy gets a grey diagonal DRAFT COPY watermark drawn into the PDF bytes of every page. Issuing soft-locks via a per-issue receipt (_issueId); unfreezeIssue releases ONLY that issue's comments and they remember (wasIssuedOnInstance). The issue log is IN-APP ONLY — never printed, never exported. Working-copy sheets import with a warning and a permanent WORKING COPY marker that DOES print. undoImportBatch walks contractors[].deficiencies AND generalDeficiencies (flat-scan defect fixed S509) and clears the batch's comment tombstones. Amendment notes NO LONGER print (_crbAmendNote retained unused, do not re-wire without Mark). (Mark, S509)
stampThreadIssued
unfreezeIssue
countFrozenByIssue
_logIssueEvent
_ensureIssueLog
getIssueEvents
openIssueHistory
onIssueHistory
_issuedCopy
workingCopy

@ Header Help label S512 (Mark, explicit go on the LOCKED headerEngine2.js). The word "Help" is rendered by CSS on the button (#btn-help::after), NOT in the icon HTML — every host rewrites that HTML when it toggles the unseen dot via setControlIcon('help', ...), so a label placed there would vanish on the first dot flip. Do not move it into the icon strings. The engine now applies each action's configured id to icon buttons (S512); the stylesheet's #btn-help rules depend on that and had never matched anything before it. Measured cost: Help folds to the drawer at 1131px instead of 1098px.
#btn-help::after

@ S511 drawing fidelity + seal visibility (Mark). Tiled sheets stitch full-res from the Azure tile pyramid at export (_stitchTiledDrawing) — before this they printed from the 400px thumbnail. Uploads store PNG (one lossy generation total) at an 8192px cap (iPhone ceiling obsolete; 24x36 now 227 DPI). Seal poll heals painted-vs-data mismatch (covers no longer vanish on same-sheet reopen). #dv-seal-layer .seal-mark{overflow:visible} keeps the floating edit chrome unclipped. Export DPI labels state the achievable ceiling per sheet size.
_stitchTiledDrawing

@ Clickable report photos S512 (Mark) — Diesel report photos wrap in <a href> to opaque https://files.arencon.app/p/{token} minted via POST /mintlinks (Supabase JWT); the capture export bakes them into PDF /Link annotations. Raw R2 URLs are never emitted (FRT privacy rule). No token -> the photo renders unlinked; an export is never blocked by the link service.
_lnk
buildPhotoHrefs
mintTokens
toBucketKey

@ S512 header button shadows (Mark, demo-approved). Every shared-header button reads pressable: .hbtn CTAs and the .chip user chip carry engine-owned hardcoded shadows in both themes (icons/.hicon had them since S249k). Box-shadow only — icons, markup and colours untouched. No host-token indirection (S504 lesson). tools/gated_push_check.sh is the mandatory mechanical gate: every push routes through it; a blocked gate kills the entire batch (born from the S511 push-despite-block failure).
gated_push_check

@ S513 viewer-header unified chrome (Mark). Every dv-toolbar header button (ids read from live markup, not guessed) shares one 34px/r8 spec with the adaptive --b-btn-shadow in both modes; #dv-help-btn styled to match the main header help. Box/spacing/shadow only — icons and markup untouched.

@ S514 (Mark, correcting S513): viewer header buttons mirror the chrome back — --b-chrome2 surface (never white), r6, 34px, hardcoded 0 1px 4px shadow (deep in dark), native 6px toolbar gap with margins zeroed, back at 40px. Engine chrome-skin .hicon/.chip shadow fallbacks hardened from the invisible .08 to the real 0 1px 4px (the S504 weak-fallback pattern, found live on the Hub). Icons untouched.

@ S515 (Mark, 3rd-round viewer-header fix): S513/S514 selectors were DEAD — the `.drawing-viewer-overlay .dv-toolbar` ancestor matches nothing; .back-btn's own styling masked the failure. Rules now use bare unique ids. Spec mirrors .back-btn (chrome2, never white, r6, 34px, 0 1px 4px shadow, deep in dark); #dv-help-btn copies the main-header .help-q glyph (amber #E0872A, 800, white on burgundy hover). LESSON RULE: a styling change is unverified until painted pixels are checked on device — dead selectors pass every gate.

@ S524 viewer chrome = ONE shared sealed button (Mark, demo-approved 34px). The six drawing-viewer header buttons and Back render inside a sealed shadow root from lib/ui/chromeButton.js — one definition, no breakpoints, no @media(pointer:coarse), so the size cannot drift again. Their ~36 frt.css blocks were DELETED in the same push: that deletion IS the feature. Order (Mark): layers · heights · seal — more · help · theme. Upgrade MUST run before restoreDarkMode()/wireEvents(), since it swaps <button> for a <span> host and any listener bound first would be discarded. Light-DOM children stay slotted so #dv-heights-dot stays findable by id and drawings.js can still append .seal-dot. Do NOT re-add id-level chrome rules to frt.css — that is what failed eleven times.
CHROME_BTN_CSS
upgradeChromeButton
syncChromeTheme
watchChromeTheme
upgradeViewerChrome
VIEWER_CHROME_BUTTONS

@ S524c deep (nested) collapse guard + single-use _intentionalClear (Mark). Counts CONTENT at every depth instead of enumerating named containers, so a copy that keeps the contractors and empties every deficiency inside them is caught — the 7155.40 shape one level down, which every container-based guard is blind to. Generic: Diesel/Electric/future tools inherit it. DELETION MUST ALWAYS WORK: fires on the PULL path only, only while the device holds UNSYNCED work, and stands aside on any explicit deletion evidence (deleted flag / tombstone). A real deletion by a real inspector always lands. Server: _intentionalClear is now consumed by the wipe guard (single-use) — it used to persist and silently disarm the guard for that report forever. Do not make this guard block a delete; if it ever does, it is broken.
_guardDeepCollapse
_walkDeepCollapse
_contentLeaves
_hasDeletionEvidence
_deepCollapseGuardFires

@ S524 DURABLE ENTRY QUEUE (Mark, I-5 completion). Report entries now have the crash-surviving delivery photos already had. The unsent marker is written to disk in the SAME cache record as the data so the two cannot disagree; it is cleared ONLY by a confirmed cloud push; on relaunch _recoverUnsentWork() finds it and flushes — but ONLY after a cloud baseline exists (I-4), never by pushing a raw disk blob. Removing any of these re-opens the failure that lost 110 minutes of flow-test data on 7155.40.
_recoverUnsentWork
_pendingSince
_ageMin
pendingPush
pendingSince

@ S524e intentional-clear declaration + Issue-status concurrency token (Mark). DELETION MUST ALWAYS WORK: the server wipe guard refuses a save taking deficiencies 3+ -> 0, which is right for a wipe and wrong for an inspector deliberately deleting their pins. Model.declareIntentionalClear() is called INSIDE removeDeficiency/removeContractor and from both type-to-confirm reset flows, so every present and future confirmed delete is covered in one place. Single-use on BOTH ends: the server consumes the flag on the save that carried it (S524c) and the engine strips it locally on the same confirmed push, so it can never linger and disarm the guard. Verified on a scratch row: undeclared delete-all refused, declared delete-all lands, flag consumed, guard re-armed. Also: _syncIssueStatus now sends If-Match + updated_by and re-pulls on 412 — it was the last FRT writer stamping updated_at with no token, invalidating other devices' tokens. Never remove the declaration from the model's delete methods; a guard that blocks a real delete is broken.
declareIntentionalClear
_intentionalClear


@ S526 markup R2 reconcile (Mark — Ian/Stacy/Nasim field incident). The S130 load order ("IDB first, R2 only when IDB is EMPTY") means any device holding ANY IDB record for a drawing never consults R2 again — so markup authored on another device is shadowed forever and looks like data loss (sign-out doesn't touch IDB). _reconcileMarkupWithR2 runs after the instant local render: per-item union by id — adds R2 objects the device lacks, honours R2 tombstones (explicit cross-device deletes), NEVER removes a local object R2 merely lacks (absence ≠ delete, doctrine I-2), stands aside if the user has started editing. Do not restore absence-based fallback; do not make R2 win wholesale (that is the pre-S130 two-opens bug in reverse).
_reconcileMarkupWithR2


@ S527 on-screen markup diagnostic (Mark). Viewer ⋯ menu → Markup Diagnostic. Reports, in plain words, where THIS DEVICE thinks the markup is: what is on screen, what is in the device's own store, what the record points at, and what the cloud file actually contains — plus a forced "Merge cloud copy now" union escape hatch for a device stuck on a stale local copy. Exists because field tablets run the Android TWA where the user CANNOT type a URL param, so there was no way to see a tablet's state remotely (Ian's dimensions, S526). NEVER convert this to a URL-param debug path. The merge button must stay additive: adds what the cloud has, honours cloud deletions, never removes a local object the cloud merely lacks.
_showMarkupDiag
_markupDiagReport


@ S528 synchronous text commit (Mark: "words existing only on screen shouldn't be allowed at all"). ROOT CAUSE of the field comment losses (5224.51 Jul 15, 4380.24 Jul 22): every arriving burst photo calls initDeficiencies.render(), demolishing and rebuilding the comment textarea from the model, while typed words lived DOM-only behind a 500ms debounce. Single-shot camera era (6360) = one render seconds after typing = timing luck. Burst era = N staggered rebuilds mid-work = routine loss; photos survive because their pipeline is instant-durable. FIX: Model.updateObservation / Model.updateClosedNote fire ON EVERY KEYSTROKE, synchronously (the write is a trivial assignment; IDB persist stays debounced downstream). Plus Model.saveNow() on visibilitychange-hidden and pagehide. NEVER reintroduce a delay between an input event and the model write in any editor; cosmetics may debounce, the model write may not.


@ S543 Field Heights commit as typed (Mark; Ian's 7033.13 measurements). Lane C's S539 gave rows permanent ids, which stopped two devices cross-pairing rows by position. This closes the other half: until Save was pressed a typed height existed ONLY in the text box, so closing the panel, a phone call, sleep or a repaint lost a whole column with no warning and nothing to recover. _commitHeightsLive() writes through on every keystroke, on row delete, on panel close and on visibilitychange-hidden, keyed by the row's permanent data-hid, NEVER by position. Same rule as S528: cosmetics may debounce; the model write may not. Do not reintroduce Save as the only commit point in this or any editor.
_commitHeightsLive


@ S544 burst shots written to disk as the shutter fires (Mark, Lane C #1 — largest single-moment loss window in the toolkit). Until S544 a burst lived ENTIRELY in memory until Done: a crash, an OOM kill, a lock-screen eviction or a stray back-swipe part way through lost every shot in it, and holding N full-resolution frames is itself what pushes the WebView into the crash (why the capture ladder is capped, S482). Each shot now goes to its own IndexedDB (ARENCON_BURST) the instant it is taken and the full-resolution bytes are released in favour of a ~1280px preview; Done reads the originals back off disk and hands the caller the SAME File[] contract, so FRT/Diesel/Electric benefit with no change of their own. Records are marked handed-off ONLY once the tool has actually received them — anything left behind by an interrupted session is offered back at the next camera open ("Add" / "Keep for later", nothing deleted, nothing added without a tap). Cancel is an explicit discard and clears that session; a crash is NOT a cancel and must never take that path. Every disk failure degrades to the pre-S544 in-memory behaviour — never to a lost shot. NEVER return to holding the burst in memory, and never delete a record that has not been handed to a tool.
_bSweep
_bMarkHandedOff
_discardSession
_materialize


@ S544 Diesel photo rescue reading from the S537 photo store (Mark, Lane C #2). S537 built Diesel's own binary copy of every photo and filled it, but nothing read from it, so Diesel photos were still not rescuable. Diesel now runs the shared photo engine's rescue pass: a photo whose stored file has gone missing is re-uploaded from THIS device's own copy the next time the report is opened here, and a photo with no image anywhere is reported rather than silently occupying a tile. ONLY the rescue pass is on — Diesel keeps its own upload queue, and the shared uploader / pull-time re-injection walk FRT's report shape and stay dormant by design. `projectId` is a REQUIRED injection for Diesel: its report payload has no top-level id (verified against the live collect), so without it verifyR2Keys quits on its first line and the registration is coverage the code does not deliver — the S532/S540 failure mode. Do not remove the resolver, and do not register another tool without first verifying where its project id actually lives.
DieselPhotoEngine
DieselR2


@ S545 burst camera Cancel asks before discarding (Mark: "I lost photos several times because of that"). The ✕ threw away an entire unfinished burst on ONE tap with nothing in between — and it sits top-left on a full-screen camera, where a thumb rests holding a tablet and where Android's own back affordance lives, so it was hit by accident repeatedly. Confirmation is drawn INSIDE the camera overlay, not through the host dialog engine: the overlay is full-screen at the top of the stacking order and a host modal can render underneath it. One tap to confirm, never type-to-confirm; tapping the backdrop is a "no"; nothing is asked when there are no shots to lose. NEVER restore a one-tap discard here, in any tool.
_confirmDiscard
_cancelBurst


@ S546 burst recovery is immediate, cheap, and report-aware (Mark's first field test of S544). THREE corrections. (1) S544 refused to offer a shot back until 60s after it was TAKEN — a crude stand-in for "is another window still shooting". It could not tell a live window from a killed app, so it made the real case wait (Mark reopened, saw nothing, assumed failure) while not even achieving its own goal, since after 60s it would offer a live window's in-flight shots anyway. A running camera now writes a heartbeat every 5s (_bBeat) and stands down on close; a session unseen for 20s is gone. Liveness is tested DIRECTLY — never reintroduce an age-based wait. (2) The sweep loaded every stored IMAGE to read a timestamp — tens of MB on camera open, the exact memory pressure S544 exists to remove. Bookkeeping now lives in its own store (shotMeta); images are read one at a time, only for shots the user taps Add on. Never merge them back. (3) THE SERIOUS ONE: recovery did not know which report the photos belonged to, so opening a different job offered another project's photos and Add filed them there, silently. Every shot is stamped with report + tool at write time and only matching shots are offered. The stamp falls back to ?project= and the pathname so EVERY tool is guarded without waiting on its lane to opt in — do not make this host-opt-in. Unstamped pre-S546 shots are still offered, flagged "check they belong to this report", because losing them is worse than asking.
_bBeat
_bBeatStop
_bLiveSessions
_bContext


@ S547 one confirmation card for every destructive action in the burst camera. The review-screen bin was the last one-tap delete left: it removed the photo on screen with no question asked. Lower risk than the ✕ (the photo is right in front of you) but the same class of thing that cost Mark photos, and the universal confirm rule has no exceptions. Both actions now share _confirmCard. Its z-index is 30 ON PURPOSE — at 12 it rendered UNDERNEATH the review screen (z-index 20), so the review delete would have asked a question the user could not see: the exact failure avoided by not using the host dialog engine, reintroduced inside our own overlay. Anything added to this overlay must clear 20. Never restore a one-tap delete anywhere in this camera.
_confirmCard
_confirmDeleteShot


@ S548 the local photo store is ONE shared engine (lib/data/photoStore.js), lifted out of part06 where it had lived since S537. A report carries its photos inside itself as text — that is how they reach the cloud, but it is the first thing stripped when a report is trimmed and it is not a file, so nothing can re-upload from it. This keeps a real image file on the device for every photo, and that file is the only thing that can put back a photo whose stored copy has vanished. Diesel supplies its database, its photo walk and its inline field name; the engine is the same one Electric will use — Electric currently has NO local copy at all, which is exactly why its photos cannot be rescued today. NEVER re-copy this into a tool: a second implementation is the matching-copy trap and the copy is always the one that stops being maintained. Three inherited behaviours must survive: it PAUSES above the storage ceiling rather than filling the tablet (an inspector must never discover the device is full mid-inspection), it works in small bites because it runs behind someone typing, and a photo with an unreadable inline copy is marked done rather than retried forever so one bad record cannot stall the sweep. The photo walk MUST keep including deleted photos and backups — a mistaken delete is recoverable only while the image still exists.
createPhotoStore


@ S549 Photo Store Check is ON SCREEN (More menu), not a console command. The field tablets are the installed Android app: no console, no address bar. The photo store is PER-DEVICE, so running a check on a desktop answers about the desktop — the only device whose answer matters is the one that took the photos. Without this there is no way to satisfy the copy/verify/release gate before Diesel's inline photo copy is retired, and deleting the inline copy while the store is short would destroy the last local copy of those photos. Read-only by design: it counts and reports, never writes, deletes or uploads. Keep it plain-language and keep the paused state visible — a device low on storage is the main reason the store falls behind, and it must say so rather than just showing a low number.
_dslPhotoStoreCheck

# S548 — FRT burst photo batching (Mark-specified). One repaint per burst
# instead of one per photo; removes the redraw mechanism that destroyed typed
# comments on 7033.13. Removing any of these puts the per-photo repaint back.
_addPhotoFiles
_photoBatchBegin
_photoBatchPaint
_photoBatchTick
_burstCtx


@ S550 the Photo Store Check reports the number that is actually about risk. The S549 panel compared the store against EVERY photo in the report and called the remainder "not backed up" — on Mark's tablet that read "1 of 223 backed up", looked stuck, and was wrong. A photo's picture is STRIPPED when the report uploads (which is why a 223-photo report is ~1MB, not ~400MB), so a device that pulled the report down never had an image for those photos and has nothing it could copy. Verified against live data: all 208 recordPhotos in 7155.40 carry an empty picture field. Two separate questions now, and they must stay separate: photos TAKEN ON THIS DEVICE (held vs still to copy — the real risk number, because that is the window before a photo is safely uploaded) and photos whose picture lives only in cloud storage (normal, expected, not this device's job, and never to be presented as a shortfall). Never fold these back into one ratio.


@ S551 on a small screen the bar keeps the CONTROLS, not the decoration (Mark, verbatim: "I really only just need the functional buttons outside, i.e. back, arencon logo, undo/redo, and menu. Others take lesser priority"). Before this the logo held roughly a third of a phone-width bar and never yielded any of it, so undo and redo — the controls an inspector reaches for most on site — were the FIRST things pushed into the drawer, where they rendered as an unlabelled floating pair. Undo/redo are now exemptUntilLast, the protection Sign Out already had; the storage meter folds first (foldRank 5) because it is a number you look at once a month. Below 520px the logo scales to 26px; below 480px the title is hidden and its 60px reservation goes with it. NOTE: hiding the title DELIBERATELY OVERRIDES the S448/S488 lock that the title truncates but is never hidden — Mark's call, because "Diese..." says nothing the project bar underneath does not, and the room it costs is the room undo and redo need. The width classes come from the BAR's own width, never a media query: this header lives in a shadow root inside tools that are also embedded, so the viewport width is not the width that matters. Fold ranks were applied to the Diesel and Electric configs only; FRT's own block is Lane A's.


@ S552 Electric joins the shared photo store — it was the LAST tool whose photos could not be rescued at all. A picture is stripped from a report when it uploads, so without a local image file there is nothing anywhere to restore from if the stored copy goes missing; Electric kept none. It now runs the SAME engine as Diesel (lib/data/photoStore.js) and supplies only what Electric knows: its database (ARENCON_ELECTRIC v4, +photoBlobs), how to list its photos (_collectAllPhotos, mapped to .photo), and where it keeps the picture (.d). NO Diesel code was copied across — a second implementation is the matching-copy trap and the copy is always the one that stops being maintained. The sweep is called from saveState and is GUARDED (typeof check) so a save can never fail because a shared module did not load. Photo Store Check added to Electric's More menu for the same reason as Diesel: the field tablets are the installed app with no console, and the store is per-device.
_elecPhotoStoreCheck
_electricLocalBytes


@ S553 ONE resolver, and a per-photo retirement test. Retiring the inline picture from a Diesel report is a REFACTOR, not a switch: about forty places read those bytes directly and SYNCHRONOUSLY (baking a markup layer, building the PDF, uploading, making a backup), while the device's own file is an ASYNCHRONOUS read. Flip retirement on before those places can wait and they get an empty string — a blank page in a client's report, a markup baked onto nothing. resolveSrc is the single door they must all come through; its order is deliberate (inline while it exists, then THIS DEVICE'S file, then cloud — the local file is preferred over cloud because it works with no signal, which is the case the store exists for). Object URLs are minted once per photo and cached, or a 200-photo gallery mints 200 more on every repaint; release() must be called when a report closes or they pin every image in memory. retirable() gates PER PHOTO and never per device: the file must be held here AND the upload confirmed, both, because either alone is a single point of failure. Note the cloud side has ALWAYS been retired this way (_keepD drops the picture once r2Status==='uploaded'), which is why no version floor and no fleet-wide check is needed for the local side — the inline copy never crosses devices. _localSrc is a live object URL and must NEVER be persisted; _stripPhotos rebuilds photo records field-by-field, which is what keeps it out.
resolveSrc
retirable


@ S553b/S554 the inline photo copy is RETIRED, per photo, on the save path. A Diesel report carried every photo twice on the device: a real image file in the store, and the same picture as text inside the report itself — copied in full every 15 seconds by the autosave. retirePass drops the second one, but ONLY after this device's own file has been read back and attached to the photo as a live source, so at no instant is a photo without a way to be drawn. That ORDERING IS THE ENTIRE SAFETY ARGUMENT — never reverse it. Gated per photo (upload confirmed AND file held here), 25 per pass because it runs behind someone typing, and it only mutates the in-memory report so the smaller payload lands naturally on the next save. DELIBERATELY SKIPPED: annotated photos, whose inline copy is the BAKED MARKED IMAGE and not a duplicate of the original, and backup records (_isOrigBackup) which exist to be the last clean copy — both still have markup paths reading the bytes directly and must not be retired until those are routed through resolveSrc. An object URL is far cheaper than what it replaces: the inline copy is text in the JS heap re-serialised on every save, the URL points at a file the browser keeps on disk. _localSrc must NEVER be persisted; collectState and _stripPhotos both rebuild photo records field-by-field, which is what keeps it out.
retirePass


@ S555 the change journal — stage ONE of change-based sync, and RECORDS ONLY. Today a device sends its entire picture of the report and hopes. When Franz's report was wiped on 7155.40 the save that did it looked exactly like every other save from the outside: a complete document written over a complete document, with nothing recording that this one arrived carrying 0 photos where the last carried 223. The evidence had to be dug out of history rows afterwards and six flow readings were never recovered. The journal writes one small entry per save — what each collection went from and to, who was at the device, which build — and flags anything that lost a third or more at once, which is the shape a wipe makes. IT MUST NOT block or alter a save, travel to the server, or feed the merge until it has been watched against real inspections: a guard firing on a rule nobody has seen in the field is how an inspector ends up unable to save on site. It never deep-compares the report and never touches photo bytes — it counts, because it runs on every autosave behind someone typing, and every path is wrapped so a journal failure can never cost a save. `collections` is where each tool says what its own report is made of, in PLAIN LANGUAGE, because these entries are read by a person on a tablet. Surfaced as "Recent Saves" in the More menu for the same reason as the Photo Store Check: the field tablets have no console.
createChangeJournal
_dslSaveLog


@ S556 Electric's photos become rescuable. S552 gave Electric its own copy of every photo it takes, but NOTHING READ IT — coverage that looks done and is not, the same trap Diesel sat in before S544, and made worse by the Photo Store Check reporting photos as held while nothing could actually put one back. Electric now runs the SAME rescue pass as Diesel: a photo whose stored file has gone missing is re-uploaded from this device's own copy, one with no image anywhere is reported rather than left as a dead tile. ONLY the rescue is on — Electric keeps its own upload queue (R2Photos), nothing is ever enqueued into this engine, and its uploader and pull-time re-injection stay dormant because those walk FRT's report shape. `projectId` is REQUIRED here as it is for Diesel: Electric's report carries no id of its own and the Hub folder is the truth; without it verifyR2Keys quits on its first line. Electric's cloud layer has no post-load callback, so the pass waits for the report to actually be up and runs late on purpose — an inspector opening a report must never be made to wait on a network sweep.
_elecRescue


@ S558 checklist items are reassign destinations (Mark, for Franz's mis-filed photos). Diesel's reassign has been a correct MOVE since S533 — same record object relocated, detach by identity, refuses to place if the detach did not find it — but the destination list offered record boxes, flow tests and deficiencies and NOT checklist items, which is the commonest mis-file on a commissioning job: a photo taken against the wrong checklist line, or dropped into Site Photos when it belongs to one, could be moved anywhere except where it belongs. Labels carry the item number and de-duplicate if two resolve to the same visible text, so two lines can never be confused in the picker. The placement branch creates the item's photo array when it has never held one — by that point the detach has already succeeded and the photo is in NO list, so it must land or it is lost. NOTE FOR ANY FUTURE SESSION: a duplicate _pgReassignSelected was very nearly shipped in S557 from a stale roadmap note claiming reassign was outstanding. It would have overridden the working S533 implementation silently, and the symbol gate CANNOT catch that — nothing disappears when a second definition is added. Read the live file before building anything the roadmap calls missing.
_CLDEST


@ S559 part06.js is FOUR FILES (part06.js + part06b/c/d.js). It had reached 9,601 lines and 547KB — five times the next largest file in the tool and the hardest thing in the codebase to work in safely. Cut at top-level boundaries ONLY; nothing renamed, exported, wrapped or moved between pieces. THEY ARE ONE SCRIPT IN FOUR PIECES, NOT FOUR MODULES: plain scripts sharing one global scope, loaded in order b then c then d immediately after part06.js. LOAD ORDER IN diesel-app/index.html IS LOAD-BEARING — change it and functions vanish for whoever runs first. HOW IT WAS PROVEN, and how any future re-cut must be: (1) joining the four files back together reproduces the original BYTE FOR BYTE, (2) each piece passes node --check standalone, (3) the gate's OWN js_symbols extractor run across all four files together shows 1381 symbols before and 1381 after, 0 lost, 40 protected symbols present and 0 lost. THE ONE-FILE GATE BLOCKED THIS PUSH on 29 protected symbols and was right to by its own rules — it compares one file against one file and cannot express a move between siblings. That block was overridden on the strength of proof (1)+(2)+(3) and disclosed to Mark, NOT worked around silently. Anyone splitting a file again must produce the same three proofs; --kill alone is not sufficient and never was.

@ S556 the integrity check is ON SCREEN (More menu), not a console command. The field tablets are the installed Android app: no console, no address bar. The check answers "are this report's photos and records intact on THIS device" — per-device by nature, so it must run where reports are made. Read-only: reports, never repairs. The five dim-on-desktop controls (remove/dup deficiency, tag removers, sr-pin) rest at full opacity on coarse pointers.
mobile-integrity-btn
_frtIntegrityPanel


@ S560 annotated photos and backup records join retirement, and the reload gap is closed. hydratePass fixes a real gap in S554: _localSrc was only set at the MOMENT of retirement, so after an app restart a retired photo had an empty picture field and nothing pointing at the file on disk — offline, a broken tile for a photo the device holds. Hydration now runs post-load (part02 kick) and at the top of every retire pass, so retirement can never outrun reattachment. The two S554 skips were lifted DELIBERATELY and differently: backup records (_isOrigBackup) retire under the standard gates because their inline copy is the same clean pixels the confirmed /original/ upload holds — a true duplicate; annotated photos retire ONLY when their stroke objects (p.mk) exist, because with strokes the marked image is a COMPUTATION (clean original + strokes recomposites anywhere) while a legacy baked-only photo's inline copy can be the LAST copy of the markup in existence — cloud /original/ is clean — and is kept unconditionally. Never collapse these two rules into one. The bake loader (_dslLoadBakeImage, part06c) accepts blob: object URLs and the backup's _localSrc as local sources, so re-baking works offline on retired records.
hydratePass


@ S561 the R2 orphan report is on screen (Cloud Storage Check, More menu). The report function itself (_dieselR2OrphanReport, part06d) is UNTOUCHED — it already returned {bucket, orphans, missing} and just had no face except the console, which the field tablets do not have. Read-only like the other panels. The PURGE stays a console operation DELIBERATELY: deleting cloud objects is not reversible and belongs at a desk with the full list in view, not behind a tap on site — do not add a purge button to this panel. Missing files are described with their remedy (the rescue pass re-uploads from a device still holding the picture); stranded files are described as a storage cost, not a correctness problem.
_dslOrphanPanel

@ S557 dimension loupe + adjust-then-place (Mark-specified after team feedback "insanely hard on touch"). The loupe magnifies what the fingertip covers, during both the measuring drag and endpoint adjustment, coarse pointers only. The adjust stage makes the lift non-final: endpoints stay grabbable, pan/zoom allowed, nothing commits until the green check — also the only way to measure a span longer than one drag. Removal puts back blind aiming and lift-is-final.
_drawLoupe
_dimEnterAdjust
_dimAdjustFinish


@ S562 the three small carried items, closed. (1) PLACARD SCAN AND THE PUMP BOX: the scan looked ONLY in its own Placard box and answered "Capture a placard photo first" while the inspector's placard shot sat in plain sight in the Pump box — the commonest mis-file on site, and silently ignoring a visible photo reads as the tool being broken. It now falls back to the MOST RECENT non-deleted Pump-box photo and SAYS SO in a toast, so a wrong guess is visible rather than silent; the existing preview/confirm still gates every value before anything is written, so scanning a photo of the pump body costs one worker call and an obviously-wrong preview, never a wrong report. The tab's own placard kind still wins whenever it exists (S338 per-pump scoping unchanged). Photo LOSS on the scan path is a SEPARATE scoped defect (SCOPE_PLACARD_SCAN_PHOTO_LOSS.md, Option C investigate-first, Mark present) and was deliberately NOT touched here. (2)+(3) ONE MEANING PER GLYPH in the flow-equipment photo viewer: the per-photo button was a ✕ titled "Remove" in the same viewer as the header's ✕ that means CLOSE — two identical glyphs, opposite meanings, one destructive — and "Remove" understated a project-wide soft delete. It is now the bin glyph titled "Delete photo (moves to Recently Deleted)", matching the lightbox and the rest of the toolkit: bin deletes, ✕ closes, ⇄ moves (its title now says it keeps the photo). Confirm modals unchanged and verified present on every delete path including the lightbox's.


@ S563 photos taken before the project folder was known finally get uploaded — and the placard photo-loss scope is CLOSED. INVESTIGATION RESULT, recorded so nobody re-opens it: the loss described in SCOPE_PLACARD_SCAN_PHOTO_LOSS.md ("add a placard photo, scan, photo gone, not in Recently Deleted") was ALREADY FIXED at S488. ArcPhoto.mint (lib/data/photoMint.js) now enqueues the upload and calls saveState on the next macrotask, so a photo is durable AT BIRTH and no call site can forget — the ~30 call sites that did photos.push(mint(...)) and nothing else were the whole mechanism, and a refresh or a cloud merge in that window destroyed the photo. Verified by running the real mint: photo in the array at save time, one saveState, one upload queued. The scope doc is STALE — do not chase model/scan code for this again. WHAT S488 COULD NOT COVER, and this fixes: _r2EnqueuePhoto returns early when _r2FolderId is null, so a photo taken before Hub init completes (or in standalone, before the report is later opened from the Hub) is saved locally and then uploaded by NOTHING. It is not lost, but it lives on ONE device forever, and the rescue pass cannot see it either — that pass verifies existing cloud keys, and a photo with no key is invisible to it. _dslUploadUnsentPhotos is the manual Re-upload All narrowed to exactly those photos and run automatically once the folder is known. Deliberately narrow, keep it that way: no cloud key at all, still carries its picture, not deleted. A retired photo can never be in this set — it has no bytes here AND by definition a confirmed upload.
_dslUploadUnsentPhotos
@ S568-S571 pin integrity + intent gate (Mark). A pin drag ANYWHERE requires the 500ms armed hold (blue glow) — S331w's "one finger on a pin is ALWAYS a drag" caused every recorded teleport (17 events, 5 inspectors) and must never return. Off-sheet commits are REFUSED and restored, never clamped onto the edge; a pinch mid-drag KEEPS a deliberate placement (validated) — it never discards it and never drags the pin. Every pin write logs to window._frtPinWriteLog (on-tablet, no console needed). Tiled sheets: pins x1.2 at tile level L3, x1.3 at L4+ (TiledPdf.getLevel).
_pinDragCapture
_pinDragRestore
_pinCommit
_pinViewerWriteLog
_pinDragLastFx
_frtPinWriteLog
getLevel
_liveLevel
@ S567 thread amendability rule (Mark-approved fix). _threadEntryAmendable is THE model-side answer editThreadEntry/removeThreadEntry ask; it mirrors crbThread _actionRow (amendable = not printed-in-issued-report, or deliberately unlocked). Model and buttons must move TOGETHER or issued records can be silently altered (looser) or editing silently dies again (stricter — broken S500-S566).
_threadEntryAmendable
@ S566 activity modal module (deficiency split stage 1). frt/js/ui/activityModal.js owns _showActivityModal; deficiencies.js imports it back. Verbatim move, reassembly-proved.
_showActivityModal
activityModal

@ S587 on-tablet Pin Write Log (Mark: the crew hits pin problems in the field and only a PC console could read the evidence). A read-only ... menu panel showing every accepted/refused/ignored/disarmed pin write this session, with Copy report. Field tablets run the Android TWA with no editable address bar, so a menu row is the ONLY way in - never a URL param. Removal returns the firm to month-later forensic digs.
_showPinWriteLog

@ S608 (Lane A) FRT sync self-report + unhangable boot (Mark's item 3: "FRT adopts the shared sync engine"). The listening loop now records WHY on every beat (_frtTickDiag ring diary, readable on-tablet from the cloud-dot popup — field tablets have no console), a watchdog releases a hung pull instead of going deaf for the session, network calls are time-bound, unsent work pushes BEFORE any pull can touch it, and the S605 stats-based re-arm keeps this device's newer entries from losing to the push dedupe forever. Boot: every network step is time-bound (_bootStep), a stalled sign-in works from the device copy instead of bouncing to a login page the connection cannot load, a timed-out boot pull is NEVER treated as an empty cloud (no fabricated blank reports — _bootPullRetry keeps asking), and a 25s watchdog forces the local render up rather than a page that never finishes. Notable outcomes go off-device to sync_diag. Harnesses frt/tests/sim/{tickhealth,bootstall,stalemate}.mjs read the shipped code and fail if any of this regresses. Removal returns FRT to the four-indistinguishable-faults era that cost Diesel nineteen sessions.
_frtHeartbeatTick
_frtTickDiag
_frtTickLog
_frtSyncDiag
_frtWithTimeout
_bootStep
_bootPullRetry
_bootPullTimedOut
_frtBootLog

@ S608 (Lane A) FRT typed photo fields in the shared merge (the S605 treatment; Mark's item 3). FRT photo dirtiness is judged only on what a person can change — caption, rotation, delete markers, and the never-bake markup vectors — never on the device-only binary the cloud strips, which had every photo reading "locally edited" forever so another tablet's caption edit or delete could never land. _lwwStripFields serializes non-scalar typed fields canonically so a MOVED mark counts as an edit. Deficiency/observation entries deliberately left untyped until Lane C closes the deficiency-propagation investigation (see frt/tests/sim/deficsync.mjs). _lwwReplay/__lwwTestHook let the harness exercise the SHIPPED merge with no I/O.
_lwwReplay
__lwwTestHook

@ S608 (Lane A) FRT live update on the shared engine (Mark's item 4). initLiveUpdate replaces the S207 "Update ready / Refresh / Not now" banner + bottom-right indicator, which are REMOVED at Mark's explicit direction ("its old S207 banner + indicator must be removed") — do NOT restore _showUpdateReadyBanner/_showUpdateReadyIndicator/_doUpdateReload from history; the engine owns listening, staging, safe moments and the swap, and FRT supplies only flush (Model.saveNow — the S162 primitive, preserved), isBusy (viewer open / photos in flight) and capture (tab + scroll). The tab now lives in the address bar (?tab=, written by switchTab, read by _restoreView), so ANY reload — refresh, update swap, crash — returns the inspector to the tab they were on instead of Project Info. TWA users cannot edit URLs; the app writing its own state is the sanctioned path.
initLiveUpdate
restoreAfterUpdate
_restoreView

@ S613 (Lane A) emptiness ignores values nobody typed — the FRT ghost-row fix. FRT stamps defaults onto every row at load (contractors get a palette colour, observations get priority/tradeSource/repeatCount, deficiencies get a migration flag), and the merge's emptiness test read those machine-written values as an inspector's work. A genuinely blank row could therefore never be recognised as empty: it survived absence-never-deletes and unioned into everyone's copy of the report — the same ghost-row machine as Diesel's 21→32, with FRT's own defaults as the cause. _lwwItemEmpty now takes a per-family `defaults` map declared in _LWW_SPECS and VALUE-MATCHES it: `priority` is ignorable only while it still reads 'high'; the moment an inspector sets 'low' the row is content again. '*' means machine-chosen and never typed. Tools that declare nothing are untouched — Diesel's behaviour is unchanged (tools/sim/converge.mjs 50/16 before and after). THE RISK THIS CARRIES, and the guard on it: widening "empty" buys ghost removal at the risk of discarding an inspector's in-progress row, which would be far worse than the bug. frt/tests/sim/converge.mjs check R builds rows through Model's OWN creation calls and asserts they survive a pull that has never seen them; it was proved to have teeth by a negative control (an over-aggressive defaults list makes all three R checks FAIL). Never widen a `defaults` map without re-running that control. Removal returns FRT to un-droppable blank rows spreading across every device.
_lwwItemEmpty

@ S622 stamp-conservation sync engine (Mark, 06 Aug 2026: "root cause, permanent, commercial grade" — the scalar/fieldMap loss arc, field-verified S622f)
_lwwRevertDirtyKept
_resolveSpecConflicts
_pushRun

@ S622g pump-type hard lock + 3pt/7pt colour sectioning (Mark, standing queue item; Level 2 approved 06 Aug 2026)
_ttHasPerfData
_ttApplyLock
_ttCommit
.tt-locked

@ S627 (Lane A, Mark on-device 07 Aug 2026) THREE FIXES BEHIND ONE COMPLAINT — "the update button keeps coming back, and I don't see any inspector ring". (1) THE DRAWING-VIEWER BUSY GUARD HAD NEVER FIRED. isBusy/busyReason tested `dv.offsetParent !== null`, and offsetParent is ALWAYS null for a position:fixed element — which .drawing-viewer-overlay is (frt.css inset:0). The check read "viewer not visible" with the viewer filling the screen, so a tap on the update pill reloaded straight out of an open drawing, skipping the markup save that runs on viewer CLOSE — the S528 loss family, live and unguarded since S622, and the reason Mark never saw "close the drawing to apply". _dvOnScreen asks getComputedStyle + a painted rect instead of a layout side-effect a fixed element does not have; do NOT revert to offsetParent, and do NOT substitute a class check (the overlay is shown by INLINE style). (2) ONE SERVICE WORKER SERVES TEN TOOLS, so every lane's push told every FRT tablet a new build was waiting — four pump-lane pushes on 06 Aug produced four pills on devices running identical FRT code, which is how a crew learns to ignore the one pill that matters. ownBuildChanged() (engine, optional, defaults to showing the pill) + _frtOwnBuildChanged (reads FRT_BUILD out of the incoming app.js so there is no second stamp to drift) suppress only the PILL; the build still stages and still applies at the next safe moment, because shared lib/ modules are real FRT code and must land. Any failure to decide falls through to showing the pill — an extra pill is a nuisance, a missed update is a stale tool in a pump room. (3) INSPECTOR RINGS WERE INVISIBLE ON REAL WORK: the rule was "two or more IDENTIFIED people", and every ARENCON report predating spring 2026 carries pins with no author at all (1490.04: five of six). One identified inspector is now enough, rows carry their pin count, and an Unattributed line states how many pins can never carry a ring — a feature that hides itself cannot be told apart from a broken one. A pin with no author still gets NO ring: guessing who placed it would put a wrong name in a client report. (4) FRT never wrote data-theme on the root element, so every shared dialog and the whole Help panel rendered their LIGHT skin on a night-mode tool with the dark skin unreachable — the fault the pump tool fixed at S497 and the Hub carried to S622. The body class stays authoritative for FRT's own stylesheet (which has no data-theme rules); the attribute is the shared layer's copy of the same truth.
_dvOnScreen
_frtOwnBuildChanged
ownBuildChanged
_unattributedPins

@ S623 MILLISECOND CLOCK (Mark, 07 Aug 2026 — "two values typed a second apart on two devices and the earlier one won"). THREE FAULTS, one symptom. (1) THE FLOOR PINNED A SKEWED MINT. The boot pull ran through Auth.request, which never feeds the clock learner, so the FIRST value typed after opening was always stamped on the raw device clock — and the monotonic floor then held that stamp, publishing floor+1ms for as long as the skew lasted. A tablet 8s fast beat everything anyone else typed for the next 8s. This is why the S622i gross-skew fix was inert: it learned the right offset and was overruled by its own guard. The boot pull now goes through _rawFetch so the clock is known BEFORE anything can be minted, and _nowSync drops a floor standing >100ms above the corrected clock (a floor minted on a clock we have since learned was wrong). Do NOT revert the boot pull to Auth.request, and do NOT restore an absolute floor. (2) The Date header is 1s resolution, which forced a ±1500ms dead band and let every sub-second skew through; it is now a FALLBACK ONLY, superseded the moment a write lands. (3) tool_data has a BEFORE UPDATE trigger (NEW.updated_at = now()) and both write paths already ask for the row back, so every save returns a Postgres timestamp at microsecond resolution — verified live 07 Aug. _learnServerStamp pairs it with the local send/receive times, keeps the LEAST-DELAYED sample (NTP's minimum filter, because a median averages good samples with congested ones), applies a correction only when it exceeds half the round trip, and SHRINKS it by that uncertainty so a correction can never overshoot into artificial future — overshoot is what defeated the S622i centring attempt, and under-correcting only costs a device a race it won by less than its own network latency. RESIDUAL, stated rather than hidden: the correction is only as tight as the round trip (~100-200ms in the field), and stamps published before a correction stay in the cloud. THE PROOF: tools/sim/clockskew.mjs — the first harness giving each device its own clock AND the mock server a real one; 7 checks including two negative controls (agreeing clocks must not acquire artificial skew; a device must still overwrite its own pre-correction value) and a SELF-VALIDITY assertion that refuses to score a race whose real gap exceeded the skew under test. That assertion exists because this probe went toothless once mid-session: modelling the boot pull stretched the gap past the skew and the 900ms check started passing on the broken engine — the same way three previous scalar fixes shipped green and failed in the field. Never delete the validity assertion.
_learnServerStamp
_learnFromRows
_applyOffset
_parseSvrIso
_nowSync

@ S624 HEARTBEAT LIVENESS (Mark, 07 Aug 2026 — device and-ceerf7 stopped emitting to sync_diag entirely at 21:22Z while the phone and PC kept syncing; a typed value stranded; only a refresh recovered). THE GAG: _syncHeartbeat opened with `if(_syncLock || _heartbeatRunning) return;`, raised the flag, awaited CloudSync.heartbeatTick() and lowered it after. A REJECTED tick was always survivable — the catch ran and the flag came down. A tick that NEVER SETTLES was not: a hung fetch (WiFi handover, captive portal, dying pump-room signal) neither resolves nor rejects, so the flag stayed raised forever, the 15s timer kept firing, and every beat returned AT THE GATE — before any logging, which is why a device in this state cannot even report its own condition. The engine's TICK_WATCHDOG_MS is no help whatsoever: it guards a flag INSIDE heartbeatTick, and the outer gate prevents heartbeatTick from ever being called. FOUR LAYERS, because any one alone leaves a hole: (1) HB_TICK_TIMEOUT_MS races the await and the flag is released in a FINALLY, never on the happy path only — a throw between raise and lower was the same permanent gag by another route; (2) HB_GAG_WATCHDOG_MS watchdogs the FLAG ITSELF at the gate and force-releases with outcome 'heartbeat-gag-released', so a failure nobody imagined still breaks out inside a minute; (3) _hbCheckLiveness on tab-visible/focus/online restarts a timer that STOPPED FIRING — a timeout cannot rescue a beat Android froze while backgrounded and never resumed; (4) visible staleness, because a stalled device that looks healthy is the real hazard. THE STALENESS THRESHOLD IS DELIBERATELY NOT A MINUTE COUNT: _hbStaleThresholdMs reads the scheduler's own declared interval (ArcSyncCadence.desiredIntervalMs) and flags three intended checks missed, with a floor — so it follows the cadence when it is retuned and when Realtime makes scheduled beats rare. Never replace it with a constant. NOTE THE MOBILE TRAP: diesel-01.css S267 force-hides #last-sync-text on narrow screens, so on the TABLET — the device that actually stalls — the warning would never have been seen; the .is-stale rule overrides that hide and must not be removed. Amber not red: the data is safe, it just has not travelled, and reserving red for real loss keeps red meaningful. Ported verbatim to Electric, which carried the identical shape and therefore the identical fault. Liveness only — nothing here touches stamps, merge outcomes, the collision door or the push dedupe. THE PROOF: tools/sim/heartbeatgag.mjs extracts the heartbeat region VERBATIM from the shipped file and runs it against a promise that never settles; the fail-first arm extracts the same region from the PRE-FIX commit, so BOTH arms run real shipped bytes rather than a transcription. Pre-fix 2/10, fixed 10/10, including a negative control that three healthy beats emit no liveness telemetry at all. KNOWN, DECLARED: blur/look-now pulls do NOT share this gate (they call the engine directly with their own time-based throttle) — they hang on the same dead network but never gag permanently; and _syncLock is declared and read at the gate but never set anywhere in either tool.
HB_TICK_TIMEOUT_MS
HB_GAG_WATCHDOG_MS
HB_STALE_FLOOR_MS
_hbDiag
_hbStaleThresholdMs
_hbIsStale
_hbStaleMinutes
_hbCheckLiveness
_hbWireLiveness
reportDiag

@ S625 STAMP PURITY + PERSISTENT LEDGER (Mark, 08 Aug: "stop patching and fix it at root"). ONE LAW: A VALUE NEVER WEARS ANOTHER VALUE'S STAMP. sync_diag 05:26:39Z proved the violation — IP's 528 carried the millisecond-exact stamp of AD's 25, then olderWon:true, then a permanent split refresh could not heal. Mechanism: merge3's _fts max-merge dresses values in the other side's stamps (its own comment admits it), and BOTH 412-door ledger assignments took that dressed document as the stamp ledger with no value-alignment — the pull path had the honesty revert, the 412 path did not. A narrow two-person race almost always 412s on the loser's side, which is exactly "narrow gaps often fail, wide gaps work". _alignSpecStamps re-asserts the law over every spec scalar and fieldMap key after ANY 412 merge: merged value == mine → my recorded stamp travels; == theirs → theirs; == neither → 0, "entry time awaits its mint" — an honest zero loses until the next save mints truth, a borrowed stamp wins races it has no right to. Times are MOVED, never minted at merge — the dirty?now fabrication has been killed four times and must never return. Equal-stamp wreckage (each device lawfully preferring its own copy, forever) heals by deterministic tie-break on canonical value order — identical on every device, NOT local-wins, because local-wins IS the deadlock — and a tie-kept local counts keptLocalNewer so the heal is pushed. PERSISTENT LEDGER (OPEN 3 root): _lastStampedLocal died with the process, so a reopened app held the typed value with no record of WHEN — an unstamped value loses every time, which is why the offline revert was reliable, not intermittent. The ledger now rides in syncMeta beside the snapshot with the clock offset, and stampLocal persists it the moment an offline stamp pins — closed exactly where the file declared it queued. B2: an offline heartbeat return is NOT cloud contact; counting it made a stalled device refresh its own freshness pill every 15s in airplane mode. DECLARED OPEN, not claimed: after a relaunch the healed value reaches the cloud on the first save (any interaction; typing-saves makes that ~1s) — a zero-interaction boot flush is follow-up, proven missing by tools/sim/offlineboot.mjs check 3, and the stamppurity poison-heal scenario exercises the merge tie-break but cannot drive the facade push re-arm.
_alignSpecStamps
_lastPersistPid
@ S628 (Lane A, Mark 07 Aug 2026) INSPECTOR IDENTITY COLOUR — EACH PERSON PICKS THEIR OWN, FIRM-WIDE. _inspectorColor hashes a user id into a fixed 12-slot list; stable, but a GUESS, and with 17 staff over 12 slots it was guessing wrong — eleven of seventeen shared a ring colour with somebody else (pale lime: Alex/Shaun/Thomas; light lime: Donovan/Jim/Nasim/William; cyan: Anna/Franz; amber: Ian/Matt). On a job with two of them pinning, the ring asserted that two different people were one person: a signal confidently stating something false, which is worse than no signal. profiles.ring_color (migration s627_inspector_ring_color, format-checked #RRGGBB, nullable) now holds the person's own choice and follows them to every project, tool and device. inspectorColorFor() is THE resolver — chosen colour first, id-hash only for someone who has never picked — and every colour site routes through it (pin ring, Layers panel swatch, resolveInspector, setInspectorEntry). NEVER call _inspectorColor directly from a rendering path again: that skips the choice and a chosen colour silently never reaches the drawing, which is precisely the S83 failure shape (engine complete, tap never connected). The 'inspectors' listener in viewer.js is load-bearing, not a nicety: profiles resolve AFTER first paint, so without a repaint every ring stays on the fallback colour for the whole session and the picker looks broken. Palette: 48 colours, generated not hand-typed, each >=26 dE from every pin body colour, contractor colour, legacy contractor colour and both sheet backgrounds, and >=21.8 dE from each other — 21.8 being the separation the shipped 12-colour palette already meets, chosen over an invented number. 48 is the CEILING at that separation (verified by a 43k-candidate packing search over the widened L* 58-90 band); more colours require either closer colours or a second ring signal, and Mark rejected BOTH — dashed rings are REJECTED, do not re-propose.
inspectorColorFor
_inspectorChosen
