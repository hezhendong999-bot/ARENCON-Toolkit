# ═══════════════════════════════════════════════════════════════════════════
# ARENCON PROTECTED SYMBOLS — features Mark specified. THE GATE WILL NOT LET
# THESE DIE. --kill does not work on them. The ONLY way to remove one is Mark
# editing this file himself.
#
# WHY (S493): the 1000-series band grouping Mark specified in June 2026 was
# silently removed by a later session — likely a revert/cleanup that could not
# tell "feature Mark asked for" from "leftover experiment" — and had to be
# recovered from past-session search. Several features have died this way.
# Handoffs go stale; this file lives in the repo, so every session hits it.
#
# FORMAT: one symbol per line. A line starting with @ names the feature the
# following symbols belong to (shown in the block message so the session knows
# WHAT it was about to destroy). Blank lines and # comments ignored.
#
# DISCIPLINE: every new Mark-specified feature gets its load-bearing symbols
# added here AS PART OF SHIPPING — same as the cache bump. Unlisted = unprotected.
# ═══════════════════════════════════════════════════════════════════════════

@ 1000-series band grouping on the Hub dashboard (server filing order; Mark, Jun 2026, restored S493)
_bandLo
_bandLabel
_renderBanded
toggleBand
.band-header
.band-section
.band-range
.band-count
.project-grid-inner.is-banded

@ Export Project Docs — whole-project bundle from Hub detail, dashboard, and card (one export, one meaning; S488-S493)
_epdRun
_epdPickProject
exportProjectDocsFromDashboard
exportSingleProject
.epd-pick
.epd-pick-search
.epd-pick-row
.epd-pick-band

@ Backup stamping — "backed up" means a real bundle was taken (S493)
_stampExported

@ Project notes never lost to the save debounce (S493)
_notesPending
_debounceSaveNotes
_saveNotes

@ Sign-in boot path guards — a missing element must not blank the Hub (S493)
_showLoginScreen
showApp
setupKeyboardShortcuts
tryRestoreSession

@ Purge type-to-confirm — permanent delete can never be one mistap (S119)
_typeToConfirmPurge
purgeProject

@ Photo-loss protections in the shared export reader (S493 — Electric/DD photos were silently dropped)
srcOf
photoIdentityKey
bucketFromRaw
commissioningPhotos
ddPhotos

@ Photo data-integrity core — audited delete, pointer protection, tombstones (S481/S393; catastrophic if lost)
delPhotoGuarded
_protectPhotoPointer
_makePurgedTombstone
mint

@ Lightbox selection engine — pick/group/unlink canon, locked S344 (unlink is never delete)
confirmPick
ungroupActive
groupActiveId
pickCount
isPicking
_groupBounds

@ Shared photo input — Camera + Upload + Gallery standard, one implementation (S478/S479)
PhotoInput

@ Shared dialog engine — all ten families; every tool's modals route here
typeToConfirm
confirmDanger
leave
# 'form' removed from this list by Mark's explicit order, Session 590 (03 Aug 2026,
# in-conversation): the cross-device conflict modal ("This report was changed by
# someone else") is DELETED from Diesel and Electric — conflicts resolve
# automatically, newest ENTRY wins via the S583 entry-time stamps; the badge
# notification system replaces the modal as the review surface. The dialog
# engine itself and its form family remain live and untouched in
# lib/ui/dialogEngine.js and its other call sites.
pickList
progress

@ Whole-project bundle builder (shared lib; Hub + tools consume it)
buildAndDownload
defaultReadme

@ Hub edit-role via shared form family; edit-project unsaved-changes guard (S493, Mark's say-so)
openEditRole
_epSnapshot
_epReadFields
@ Seal redaction — PDF appendix covers (S492, Mark-specified; LOCKED_SEAL_REDACTION.md; decided WARN)
_drawSealCovers
_drawSealCoversCropped
_openRedactionEditor
SEAL_COVER_LABEL
@ Seal redaction visibility — markers/badges/contact sheet (S492, Mark-approved demo; LOCKED_SEAL_REDACTION_VISIBILITY.md)
_renderSealMarkersInViewer
_sealUndo
_sealSnapshot
_toggleSealEditMode
_sealApplyAll
_frtRefreshSealMarkers
_sealRows

@ Cards/List view toggle — locked June list view, recovered from pre-wipe blob 4e8837a1 and restored (S494, Mark: "This is it. Wire it")
setHubView
_hubView
.hub-view-toggle
.view-list

@ Tool filter chips — filter the board by which tool a project uses (S494, Mark-specified; search cannot do this)
setToolFilter
_toolFilter
.tool-chip

@ Admin roster fields — User #, Initials, Edit Name, Reset Password (Mark-specified June; deleted by S391 stale push; restored verbatim S494 from pre-wipe blob 4e8837a1)
openEditNum
saveUserNum
openEditInit
saveUserInit
openEditName
saveUserName
openResetPw
submitResetPw
genTempPassword

@ Modal safety — backdrop clicks never close modals; every modal carries an X routed through its own Cancel path (Mark, S494)
_injectModalX
.modal-x

@ Tools & Reports hierarchy — activation demoted to a slim settings strip, reports promoted to accent-bar rows, "+ New Report" small and quiet (never burgundy, never large — rejected twice) (Mark, approved S494, shipped S495 Lane B)
.tool-strip
.ts-lbl
.tw
.tw-sw
.rgroup
.rghead
.rg-ic
.rg-name
.rg-cnt
.btn-new
.rrow
.rmain
.rtitle
.rmeta
.rctl
.rempty

@ Deleted Reports Stage 1 — report delete is a SOFT delete (deleted_at/deleted_by), restorable from the project; deleted reports filtered at the SOURCE so they never leak into exports; permanent delete is admin-only (Mark, design S494, shipped S495 Lane B)
_loadAndRenderDeletedInstances
restoreInstance
purgeInstance
_canPurgeReports
detail-deleted-section
detail-deleted-instances
deleted-reports-count
.rrow.is-deleted

@ New Project client suggestion by number series + duplicate project-number warning — suggests clients already used under that series (canonical spelling ranks first), warns on a duplicate full number without blocking, PM defaults to creator (Mark, requested S489, shipped S495 Lane B)
_npSeriesOf
_npClientsForSeries
_npOnNumberInput
_npPickClient
np-client-suggest
np-dupe-warn
.np-suggest
.np-suggest-chip
.np-note-warn

@ Backup discipline — one-tap select of every never-backed-up project, and backup AGE tiers (30/60/90/never) as TEXT TINT ONLY, never card fills or left edges (Mark, roadmap, shipped S495 Lane B)
selectNotBackedUp
.backup-status.age-30
.backup-status.age-60
.backup-status.age-90


@ HOST CONTRACT — Diesel: functions the SHARED /lib/ modules call BY NAME (Claude, S496, after the S492 dead-code sweep deleted five of these and made the Diesel/Electric checklist photo zone inert over live data). These are NOT a Mark-specified feature — they are the wiring that makes the shared engines work. They are listed here because the failure mode is EXACTLY what this manifest exists to stop: a session greps the tool, sees zero callers, declares the function dead, and --kill lets it through. `removePhoto` is the proof — grep Diesel and it has ZERO callers, yet lib/ui/checklist.js calls it from a generated onclick. node --check cannot see late-bound globals; only a lib grep can. Before deleting ANY function from a tool that consumes /lib/, grep EVERY lib module.
_aConfirm
_collectCloudState
_dslLoadBakeImage
_dslMarkupPersist
_dslMarkupRevert
_dslRefreshPhotoSurfaces
_galleryReuseChecklist
_isPhotoDeleted
_isRealImageBlob
_phSrc
_photoSrc
_r2EnqueuePhoto
_r2Fname
_rebuildMkDisplay
_renderPhotoGallery
debounceAutosave
handleDrop
openCameraBurst
openLightbox
openLightboxMarkup
removePhoto
saveState
setStatus
showToast
toggleItemDetail
triggerPhoto
updateCompletionOverview
updateProgress
updateVerdict

@ HOST CONTRACT — Electric: callbacks the sealed header engine v2 and the shared photo input invoke by name via deferred window lookups (Claude, S496). Same reasoning as the Diesel block above: these are called from lib/ui/headerConfigs.js and lib/ui/photoInput.js, not from Electric itself, so a reference scan of Electric alone shows them as unused. Deleting any of them silently breaks the header controls or the photo zone.
handleFiles
handleFlowTestDrop
handleFlowTestDropPld
issueReport
goBackToHub
exportPDF
downloadJSON
importJSON
_r2ReuploadAll
_r2CleanupOrphans
resetCurrentPage
resetAllPages
_openToolQR
toggleDarkMode
cycleTextSize
signOutSession
updateDarkToggleIcon

@ EXPORT QUALITY — FRT (Mark-specified, S496). Per-export photo/drawing quality tiers plus PER-SHEET drawing DPI overrides, replacing the flat 5 MP drawing cap that rendered every sheet at ~63 DPI regardless of size. Mark specified 175 DPI as the baseline and specifically required per-sheet control ("some dwg doesnt need high dpi"). _qDrawingMaxPx also carries the device memory budget that stops a large sheet crashing an export on a field tablet — removing it reintroduces a crash-on-site risk, not just a quality regression. The seal-tile slider markup (.exq-tile / data-exq-sl) is the ONLY surface for the per-sheet control; deleting it strands the feature with no UI.
PDF_TIERS
PDF_SHEET_IN
_qTierDef
_qPeakMB
_qBudgetMB
_qDrawingMaxPx
_applyExportQuality
_getQualityNotes
_qDwgTierDef
@ Hidden-report badge — a tool switched OFF that still holds reports shows a count badge; work is never silently hidden. enabled_tools filters the SCREEN ONLY, never exports (Mark, S495)
_toolReportCounts
.tw-hid
.tw.has-hidden

@ Export must contain report data — _fetchProjectToolData reads the array execute() returns; a failed read must NOT stamp the project as backed up (S495 bug fix)
_fetchProjectToolData

@ Backup honesty — an export containing zero reports downloads but is NOT stamped as a backup, and read failures are reported; a stamp on a hollow file is a false all-clear (S495, root cause of the S127/S495 hollow-export class)
_exportHasReportData

@ Canonical tool order — report groups, dashboard tool pills and Deleted Reports all follow AVAILABLE_TOOLS (FRT, DFP, EFP, IST, OBC, DD), never enabled_tools storage order (Mark, S495)
_orderTools

@ Retention Stage 2 — 90-day countdown chips on deleted reports and trashed projects; nightly reporter tools/retention_sweep.mjs has NO delete verb (reports only, anomaly cap goes red); purge audit records the purger via the existing DELETE snapshot trigger (Mark, design S494, shipped S495 Lane B)
RETENTION_DAYS
RETENTION_WARN_DAYS
_retentionOf
.ret-chip
.ret-warn
.ret-expired

@ One tab per report — clicking a report already open FOCUSES its tab instead of opening a second one; two tabs on one report both autosave to the same row and the loser's work is lost silently. Middle/ctrl/right-click still opens a deliberate second copy (Mark, original design restored S495)
arencon_rpt_

@ Range bands always visible — band/tool chips count the WHOLE scope (not just unpinned leftovers), so My Projects shows its ranges; counts stay stable while a filter is active, and an ACTIVE filter narrows pinned/starred too (Mark, S495)
_chipBase
_chipScope

@ PIN REMOVED — star is the only project override. Pin was GLOBAL (one person's pin sat on everyone's board); star is per-user. Mark: explicit and final, S495. Do NOT reintroduce a global pin; if firm-wide attention is ever needed that is a separate announcement feature, not a sort order. is_pinned remains in the DB, unread.
starred-section
toggleStar
selectAllStarred

@ sw.js CACHE_NAME is a UTC TIMESTAMP, never a counter — a counter let two lanes silently revert each other's bump (near-miss S495, both picked v1174). Bump = set to current UTC yyyymmddhhmm. Do NOT reintroduce an incrementing version (Mark, S495)
CACHE_NAME

@ Print-size photo scaling — photos embed at their PRINTED 3-across cell size (photoDpi × PDF_PHOTO_CELL_IN), tier photoPx is only a ceiling (Mark's queue item 5, S497). Do not revert to flat long-edge targets.
PDF_PHOTO_CELL_IN
@ Insights panel — backup coverage, backup age and active-projects-per-person, computed from the loaded board with hand-drawn SVG donuts, no chart library (Mark, roadmap item, shipped S497 Lane B; re-appended 2x after shared-file contention dropped this block — manifest needs a mechanical merge fix)
openInsights
_donutSvg

@ What's New + Hub Guide — Help panel fed by updates.json, which is edited ONLY when Mark says so, never automatically; amber unseen-update dot per device (Mark, roadmap item, shipped S497 Lane B)
openHelp
_wnCheck
.wn-dot

@ Duplicate project indicator — later-created projects sharing a number carry an amber Duplicate chip; the first-created never does; trashed projects neither carry nor cause it (Mark, field test S497b)
_isDupProject
.badge-dup

@ Global client autocomplete in New Project — typing matches every client on record, most-used spelling first, offered never forced (Mark, field test S497b)
_npOnClientInput
_npAllClients
@ Portal shared header — index.html header is the sealed engine + lib/ui/portalHeaderConfig.js; theme on the shared device key so the portal matches every other tool (Mark, field test S497c). The hand-rolled portal header/toggle is retired and must not return.
portalHeaderConfig
.imx-head
.imx-body
@ Duplicate project numbers BLOCKED at creation (Mark, explicit S497d; supersedes S495 warn-only) + admin Client Name Cleanup + R2 Reclamation Stage 3 UI (Mark present and instructing; deletes ONLY the nightly report's still-existing candidates via the authenticated worker)
renderClientCleanup
ccMergeCluster
ccMergeManual
_ccNorm
renderReclamation
rrDelete
@ Client-name guards for everyone, not admin-only (Mark, S497e): create-time snap to existing spelling on case/punctuation match; edit-time ripple offering to propagate a corrected client name to every project spelling it the old way
_clientCanonicalFor
# ── S498 Lane B (Mark): client rename across projects + correction requests ──
openClientRename
_crnApply
_crnOnName
_notifInit
_notifRenderCard
_notifAccept
_notifAcceptAll
_notifDeclineRow
_notifReview
crn-open
notif-stack
client-rename-modal
notif-review-modal

@ Flow-point gauge/RPM photo modal — extracted to lib/ui/flowPhotoModal.js (Mark, S500; With/Without-PLD modes, reading tags, reassign, gallery reuse)
openFlowPhotos
closeFlowPhotos
_renderFlowPhotoModal
_flowPhotoAddFile
_flowPhotoIcon
_flowPhotoOpenGallery
_flowPhotoOpenReassign
_flowPhotoSetActiveMode
_GAUGE_READINGS
_GAUGE_TAG_LABEL

@ Deficiencies panel — extracted to lib/ui/deficiencies.js (Mark, S500; contractor groups, general defic, priorities, IAR, findings roll-up, All-view + CSV)
renderDeficGroups
buildDeficItem
renderGeneralDeficGroup
buildGeneralDeficItem
updateDeficSummary
_renderChecklistFindings
deficiencies
generalDeficiencies
deficPhotoTarget

@ CRB unlock / amend / revert / history — printed comments editable via deliberate unlock with a forever audit log (Mark, S500; overrides locked §1 freeze-all)
unlockThreadEntry
revertThreadEntry
_logAmend
_threadEntryAmendable
crbt-unlock
crbt-revert
crbt-history
amendedAfterIssue

@ CRB import-batch undo — pull a whole contractor-PDF import back out in one tap; hard-removes untouched rows, preserves rows you've reviewed (Mark, S500-C)
undoImportBatch
_noticeUndo

@ CRB report-side amendment printing — an amended-after-issue comment prints its original issued wording in the next report, so the paper record isn't silently changed (Mark, S500-D)
_crbAmendNote
crb-amend

@ Diesel deficiency status pill — FRT S263 port (Mark-locked, S502; one tappable pill, 5-state popover, rec/site classification flags, data mapping)
_deficStatusPill
_deficPickStatus
_deficChoice
_deficEnsureMenu
_deficTogglePillMenu
_CV_CHOICES
isRecommendation
isSiteRecord

@ CRB stale-sheet acknowledgement — a contractor PDF answering an older report than the project's current round no longer hard-blocks; warns and files on the current round by default, backfill-anyway available (Mark, S500-E)
_confirmStale
_runImport

@ Diesel Help & guide — shared searchable Help engine mounted in Diesel's sealed dialog (ArenconDlg.panel), Diesel's own cards in lib/ui/dieselHelpCards.js, "?" button folds with the icon trio. openHelp/_helpSetDot live in BOTH twins (monolith + diesel-app beta) and are invoked by name from the shared header config's onHelp — a grep of Diesel alone shows the header wiring as the only caller. Coming-soon fallback if cards fail to register. (Mark, S505, help/guide roadmap seed)
openHelp
_helpSetDot

@ FRT Help & guide — shared searchable Help engine in the shared sealed dialog, FRT's own cards in lib/ui/frtHelpCards.js, scope 'FRT' (one panel = one scope, never mixed). openHelp/_helpSetDot are invoked by name from the shared header config's onHelp, so a grep of app.js shows the header wiring as the only caller. Coming-soon fallback if cards fail to register. (Mark, S505g)
openHelp
_helpSetDot

@ CRB re-import diff — a re-sent contractor sheet is reconciled instead of silently dropped. Deliberate removal of an IMPORTED comment writes a tombstone (wording included); a re-import then offers it back one-tap, and declining records the declined wording so an identical re-send stays quiet while a changed one re-surfaces. Reworded answers split on issued state: an unissued draft offers Replace/Keep-both, an ALREADY ISSUED answer is never overwritten and lands as a new round. findLiveResponseByDedupe MUST walk contractors[].deficiencies AND generalDeficiencies — there is no flat _project.deficiencies. _writeRow is the single writer for every branch; do not add a second one. (Mark, S508)
_ensureCommentTombs
_normTombText
_writeCommentTomb
findCommentTomb
dismissCommentTomb
clearCommentTomb
tombDecisionFor
findLiveResponseByDedupe
replaceUnissuedImportedDraft
_classifyDupes
_resolveDiffQueue
_confirmOfferBack
_confirmReworded
_writeRow
_diffOverlay
_quoteBlock
_sizeChartsForPrint
_dslVerdict
_dslVerdictFacts

@ Help system S509-S510 (Mark) — task-style guides + contextual "?" across the platform. Dashboard (23 cards) and Hub page (13) rebuilt with real-screen replica art and Mark's invented cast — NO real staff/client/project data may return to any guide drawing. Diesel rebuilt task-style (15 cards); Electric wired for the first time (8 cards, verified surfaces only — expand with the photo-architecture port, never ahead of it). Existing card ids, search-term strings and dates are carried VERBATIM on any future rebuild. In-place "?" buttons: Hub New Project + Insights, FRT drawing-viewer toolbar (#dv-help-btn) and pin editor (.dfx-ed-help) via the window._frtHelpAt bridge in frt/js/app.js. tools/check_help_art.py must pass (0 collisions) after touching any guide drawing.
_frtHelpAt
.dfx-ed-help
ART.hpPage
electricHelpCards

@ Issue soft-lock lifecycle + working-copy imports + DRAFT watermark. Issuing is asked BEFORE export generation; a working copy gets a grey diagonal DRAFT COPY watermark drawn into the PDF bytes of every page. Issuing soft-locks via a per-issue receipt (_issueId); unfreezeIssue releases ONLY that issue's comments and they remember (wasIssuedOnInstance). The issue log is IN-APP ONLY — never printed, never exported. Working-copy sheets import with a warning and a permanent WORKING COPY marker that DOES print. undoImportBatch walks contractors[].deficiencies AND generalDeficiencies (flat-scan defect fixed S509) and clears the batch's comment tombstones. Amendment notes NO LONGER print (_crbAmendNote retained unused, do not re-wire without Mark). (Mark, S509)
stampThreadIssued
unfreezeIssue
countFrozenByIssue
_logIssueEvent
_ensureIssueLog
getIssueEvents
openIssueHistory
onIssueHistory
_issuedCopy
workingCopy

@ Header Help label S512 (Mark, explicit go on the LOCKED headerEngine2.js). The word "Help" is rendered by CSS on the button (#btn-help::after), NOT in the icon HTML — every host rewrites that HTML when it toggles the unseen dot via setControlIcon('help', ...), so a label placed there would vanish on the first dot flip. Do not move it into the icon strings. The engine now applies each action's configured id to icon buttons (S512); the stylesheet's #btn-help rules depend on that and had never matched anything before it. Measured cost: Help folds to the drawer at 1131px instead of 1098px.
#btn-help::after

@ S511 drawing fidelity + seal visibility (Mark). Tiled sheets stitch full-res from the Azure tile pyramid at export (_stitchTiledDrawing) — before this they printed from the 400px thumbnail. Uploads store PNG (one lossy generation total) at an 8192px cap (iPhone ceiling obsolete; 24x36 now 227 DPI). Seal poll heals painted-vs-data mismatch (covers no longer vanish on same-sheet reopen). #dv-seal-layer .seal-mark{overflow:visible} keeps the floating edit chrome unclipped. Export DPI labels state the achievable ceiling per sheet size.
_stitchTiledDrawing

@ Clickable report photos S512 (Mark) — Diesel report photos wrap in <a href> to opaque https://files.arencon.app/p/{token} minted via POST /mintlinks (Supabase JWT); the capture export bakes them into PDF /Link annotations. Raw R2 URLs are never emitted (FRT privacy rule). No token -> the photo renders unlinked; an export is never blocked by the link service.
_lnk
buildPhotoHrefs
mintTokens
toBucketKey

@ S512 header button shadows (Mark, demo-approved). Every shared-header button reads pressable: .hbtn CTAs and the .chip user chip carry engine-owned hardcoded shadows in both themes (icons/.hicon had them since S249k). Box-shadow only — icons, markup and colours untouched. No host-token indirection (S504 lesson). tools/gated_push_check.sh is the mandatory mechanical gate: every push routes through it; a blocked gate kills the entire batch (born from the S511 push-despite-block failure).
gated_push_check

@ S513 viewer-header unified chrome (Mark). Every dv-toolbar header button (ids read from live markup, not guessed) shares one 34px/r8 spec with the adaptive --b-btn-shadow in both modes; #dv-help-btn styled to match the main header help. Box/spacing/shadow only — icons and markup untouched.

@ S514 (Mark, correcting S513): viewer header buttons mirror the chrome back — --b-chrome2 surface (never white), r6, 34px, hardcoded 0 1px 4px shadow (deep in dark), native 6px toolbar gap with margins zeroed, back at 40px. Engine chrome-skin .hicon/.chip shadow fallbacks hardened from the invisible .08 to the real 0 1px 4px (the S504 weak-fallback pattern, found live on the Hub). Icons untouched.

@ S515 (Mark, 3rd-round viewer-header fix): S513/S514 selectors were DEAD — the `.drawing-viewer-overlay .dv-toolbar` ancestor matches nothing; .back-btn's own styling masked the failure. Rules now use bare unique ids. Spec mirrors .back-btn (chrome2, never white, r6, 34px, 0 1px 4px shadow, deep in dark); #dv-help-btn copies the main-header .help-q glyph (amber #E0872A, 800, white on burgundy hover). LESSON RULE: a styling change is unverified until painted pixels are checked on device — dead selectors pass every gate.

@ S524 viewer chrome = ONE shared sealed button (Mark, demo-approved 34px). The six drawing-viewer header buttons and Back render inside a sealed shadow root from lib/ui/chromeButton.js — one definition, no breakpoints, no @media(pointer:coarse), so the size cannot drift again. Their ~36 frt.css blocks were DELETED in the same push: that deletion IS the feature. Order (Mark): layers · heights · seal — more · help · theme. Upgrade MUST run before restoreDarkMode()/wireEvents(), since it swaps <button> for a <span> host and any listener bound first would be discarded. Light-DOM children stay slotted so #dv-heights-dot stays findable by id and drawings.js can still append .seal-dot. Do NOT re-add id-level chrome rules to frt.css — that is what failed eleven times.
CHROME_BTN_CSS
upgradeChromeButton
syncChromeTheme
watchChromeTheme
upgradeViewerChrome
VIEWER_CHROME_BUTTONS

@ S524c deep (nested) collapse guard + single-use _intentionalClear (Mark). Counts CONTENT at every depth instead of enumerating named containers, so a copy that keeps the contractors and empties every deficiency inside them is caught — the 7155.40 shape one level down, which every container-based guard is blind to. Generic: Diesel/Electric/future tools inherit it. DELETION MUST ALWAYS WORK: fires on the PULL path only, only while the device holds UNSYNCED work, and stands aside on any explicit deletion evidence (deleted flag / tombstone). A real deletion by a real inspector always lands. Server: _intentionalClear is now consumed by the wipe guard (single-use) — it used to persist and silently disarm the guard for that report forever. Do not make this guard block a delete; if it ever does, it is broken.
_guardDeepCollapse
_walkDeepCollapse
_contentLeaves
_hasDeletionEvidence
_deepCollapseGuardFires

@ S524 DURABLE ENTRY QUEUE (Mark, I-5 completion). Report entries now have the crash-surviving delivery photos already had. The unsent marker is written to disk in the SAME cache record as the data so the two cannot disagree; it is cleared ONLY by a confirmed cloud push; on relaunch _recoverUnsentWork() finds it and flushes — but ONLY after a cloud baseline exists (I-4), never by pushing a raw disk blob. Removing any of these re-opens the failure that lost 110 minutes of flow-test data on 7155.40.
_recoverUnsentWork
_pendingSince
_ageMin
pendingPush
pendingSince

@ S524e intentional-clear declaration + Issue-status concurrency token (Mark). DELETION MUST ALWAYS WORK: the server wipe guard refuses a save taking deficiencies 3+ -> 0, which is right for a wipe and wrong for an inspector deliberately deleting their pins. Model.declareIntentionalClear() is called INSIDE removeDeficiency/removeContractor and from both type-to-confirm reset flows, so every present and future confirmed delete is covered in one place. Single-use on BOTH ends: the server consumes the flag on the save that carried it (S524c) and the engine strips it locally on the same confirmed push, so it can never linger and disarm the guard. Verified on a scratch row: undeclared delete-all refused, declared delete-all lands, flag consumed, guard re-armed. Also: _syncIssueStatus now sends If-Match + updated_by and re-pulls on 412 — it was the last FRT writer stamping updated_at with no token, invalidating other devices' tokens. Never remove the declaration from the model's delete methods; a guard that blocks a real delete is broken.
declareIntentionalClear
_intentionalClear


@ S526 markup R2 reconcile (Mark — Ian/Stacy/Nasim field incident). The S130 load order ("IDB first, R2 only when IDB is EMPTY") means any device holding ANY IDB record for a drawing never consults R2 again — so markup authored on another device is shadowed forever and looks like data loss (sign-out doesn't touch IDB). _reconcileMarkupWithR2 runs after the instant local render: per-item union by id — adds R2 objects the device lacks, honours R2 tombstones (explicit cross-device deletes), NEVER removes a local object R2 merely lacks (absence ≠ delete, doctrine I-2), stands aside if the user has started editing. Do not restore absence-based fallback; do not make R2 win wholesale (that is the pre-S130 two-opens bug in reverse).
_reconcileMarkupWithR2


@ S527 on-screen markup diagnostic (Mark). Viewer ⋯ menu → Markup Diagnostic. Reports, in plain words, where THIS DEVICE thinks the markup is: what is on screen, what is in the device's own store, what the record points at, and what the cloud file actually contains — plus a forced "Merge cloud copy now" union escape hatch for a device stuck on a stale local copy. Exists because field tablets run the Android TWA where the user CANNOT type a URL param, so there was no way to see a tablet's state remotely (Ian's dimensions, S526). NEVER convert this to a URL-param debug path. The merge button must stay additive: adds what the cloud has, honours cloud deletions, never removes a local object the cloud merely lacks.
_showMarkupDiag
_markupDiagReport


@ S528 synchronous text commit (Mark: "words existing only on screen shouldn't be allowed at all"). ROOT CAUSE of the field comment losses (5224.51 Jul 15, 4380.24 Jul 22): every arriving burst photo calls initDeficiencies.render(), demolishing and rebuilding the comment textarea from the model, while typed words lived DOM-only behind a 500ms debounce. Single-shot camera era (6360) = one render seconds after typing = timing luck. Burst era = N staggered rebuilds mid-work = routine loss; photos survive because their pipeline is instant-durable. FIX: Model.updateObservation / Model.updateClosedNote fire ON EVERY KEYSTROKE, synchronously (the write is a trivial assignment; IDB persist stays debounced downstream). Plus Model.saveNow() on visibilitychange-hidden and pagehide. NEVER reintroduce a delay between an input event and the model write in any editor; cosmetics may debounce, the model write may not.


@ S543 Field Heights commit as typed (Mark; Ian's 7033.13 measurements). Lane C's S539 gave rows permanent ids, which stopped two devices cross-pairing rows by position. This closes the other half: until Save was pressed a typed height existed ONLY in the text box, so closing the panel, a phone call, sleep or a repaint lost a whole column with no warning and nothing to recover. _commitHeightsLive() writes through on every keystroke, on row delete, on panel close and on visibilitychange-hidden, keyed by the row's permanent data-hid, NEVER by position. Same rule as S528: cosmetics may debounce; the model write may not. Do not reintroduce Save as the only commit point in this or any editor.
_commitHeightsLive


@ S544 burst shots written to disk as the shutter fires (Mark, Lane C #1 — largest single-moment loss window in the toolkit). Until S544 a burst lived ENTIRELY in memory until Done: a crash, an OOM kill, a lock-screen eviction or a stray back-swipe part way through lost every shot in it, and holding N full-resolution frames is itself what pushes the WebView into the crash (why the capture ladder is capped, S482). Each shot now goes to its own IndexedDB (ARENCON_BURST) the instant it is taken and the full-resolution bytes are released in favour of a ~1280px preview; Done reads the originals back off disk and hands the caller the SAME File[] contract, so FRT/Diesel/Electric benefit with no change of their own. Records are marked handed-off ONLY once the tool has actually received them — anything left behind by an interrupted session is offered back at the next camera open ("Add" / "Keep for later", nothing deleted, nothing added without a tap). Cancel is an explicit discard and clears that session; a crash is NOT a cancel and must never take that path. Every disk failure degrades to the pre-S544 in-memory behaviour — never to a lost shot. NEVER return to holding the burst in memory, and never delete a record that has not been handed to a tool.
_bSweep
_bMarkHandedOff
_discardSession
_materialize


@ S544 Diesel photo rescue reading from the S537 photo store (Mark, Lane C #2). S537 built Diesel's own binary copy of every photo and filled it, but nothing read from it, so Diesel photos were still not rescuable. Diesel now runs the shared photo engine's rescue pass: a photo whose stored file has gone missing is re-uploaded from THIS device's own copy the next time the report is opened here, and a photo with no image anywhere is reported rather than silently occupying a tile. ONLY the rescue pass is on — Diesel keeps its own upload queue, and the shared uploader / pull-time re-injection walk FRT's report shape and stay dormant by design. `projectId` is a REQUIRED injection for Diesel: its report payload has no top-level id (verified against the live collect), so without it verifyR2Keys quits on its first line and the registration is coverage the code does not deliver — the S532/S540 failure mode. Do not remove the resolver, and do not register another tool without first verifying where its project id actually lives.
DieselPhotoEngine
DieselR2


@ S545 burst camera Cancel asks before discarding (Mark: "I lost photos several times because of that"). The ✕ threw away an entire unfinished burst on ONE tap with nothing in between — and it sits top-left on a full-screen camera, where a thumb rests holding a tablet and where Android's own back affordance lives, so it was hit by accident repeatedly. Confirmation is drawn INSIDE the camera overlay, not through the host dialog engine: the overlay is full-screen at the top of the stacking order and a host modal can render underneath it. One tap to confirm, never type-to-confirm; tapping the backdrop is a "no"; nothing is asked when there are no shots to lose. NEVER restore a one-tap discard here, in any tool.
_confirmDiscard
_cancelBurst


@ S546 burst recovery is immediate, cheap, and report-aware (Mark's first field test of S544). THREE corrections. (1) S544 refused to offer a shot back until 60s after it was TAKEN — a crude stand-in for "is another window still shooting". It could not tell a live window from a killed app, so it made the real case wait (Mark reopened, saw nothing, assumed failure) while not even achieving its own goal, since after 60s it would offer a live window's in-flight shots anyway. A running camera now writes a heartbeat every 5s (_bBeat) and stands down on close; a session unseen for 20s is gone. Liveness is tested DIRECTLY — never reintroduce an age-based wait. (2) The sweep loaded every stored IMAGE to read a timestamp — tens of MB on camera open, the exact memory pressure S544 exists to remove. Bookkeeping now lives in its own store (shotMeta); images are read one at a time, only for shots the user taps Add on. Never merge them back. (3) THE SERIOUS ONE: recovery did not know which report the photos belonged to, so opening a different job offered another project's photos and Add filed them there, silently. Every shot is stamped with report + tool at write time and only matching shots are offered. The stamp falls back to ?project= and the pathname so EVERY tool is guarded without waiting on its lane to opt in — do not make this host-opt-in. Unstamped pre-S546 shots are still offered, flagged "check they belong to this report", because losing them is worse than asking.
_bBeat
_bBeatStop
_bLiveSessions
_bContext


@ S547 one confirmation card for every destructive action in the burst camera. The review-screen bin was the last one-tap delete left: it removed the photo on screen with no question asked. Lower risk than the ✕ (the photo is right in front of you) but the same class of thing that cost Mark photos, and the universal confirm rule has no exceptions. Both actions now share _confirmCard. Its z-index is 30 ON PURPOSE — at 12 it rendered UNDERNEATH the review screen (z-index 20), so the review delete would have asked a question the user could not see: the exact failure avoided by not using the host dialog engine, reintroduced inside our own overlay. Anything added to this overlay must clear 20. Never restore a one-tap delete anywhere in this camera.
_confirmCard
_confirmDeleteShot


@ S548 the local photo store is ONE shared engine (lib/data/photoStore.js), lifted out of part06 where it had lived since S537. A report carries its photos inside itself as text — that is how they reach the cloud, but it is the first thing stripped when a report is trimmed and it is not a file, so nothing can re-upload from it. This keeps a real image file on the device for every photo, and that file is the only thing that can put back a photo whose stored copy has vanished. Diesel supplies its database, its photo walk and its inline field name; the engine is the same one Electric will use — Electric currently has NO local copy at all, which is exactly why its photos cannot be rescued today. NEVER re-copy this into a tool: a second implementation is the matching-copy trap and the copy is always the one that stops being maintained. Three inherited behaviours must survive: it PAUSES above the storage ceiling rather than filling the tablet (an inspector must never discover the device is full mid-inspection), it works in small bites because it runs behind someone typing, and a photo with an unreadable inline copy is marked done rather than retried forever so one bad record cannot stall the sweep. The photo walk MUST keep including deleted photos and backups — a mistaken delete is recoverable only while the image still exists.
createPhotoStore


@ S549 Photo Store Check is ON SCREEN (More menu), not a console command. The field tablets are the installed Android app: no console, no address bar. The photo store is PER-DEVICE, so running a check on a desktop answers about the desktop — the only device whose answer matters is the one that took the photos. Without this there is no way to satisfy the copy/verify/release gate before Diesel's inline photo copy is retired, and deleting the inline copy while the store is short would destroy the last local copy of those photos. Read-only by design: it counts and reports, never writes, deletes or uploads. Keep it plain-language and keep the paused state visible — a device low on storage is the main reason the store falls behind, and it must say so rather than just showing a low number.
_dslPhotoStoreCheck

# S548 — FRT burst photo batching (Mark-specified). One repaint per burst
# instead of one per photo; removes the redraw mechanism that destroyed typed
# comments on 7033.13. Removing any of these puts the per-photo repaint back.
_addPhotoFiles
_photoBatchBegin
_photoBatchPaint
_photoBatchTick
_burstCtx


@ S550 the Photo Store Check reports the number that is actually about risk. The S549 panel compared the store against EVERY photo in the report and called the remainder "not backed up" — on Mark's tablet that read "1 of 223 backed up", looked stuck, and was wrong. A photo's picture is STRIPPED when the report uploads (which is why a 223-photo report is ~1MB, not ~400MB), so a device that pulled the report down never had an image for those photos and has nothing it could copy. Verified against live data: all 208 recordPhotos in 7155.40 carry an empty picture field. Two separate questions now, and they must stay separate: photos TAKEN ON THIS DEVICE (held vs still to copy — the real risk number, because that is the window before a photo is safely uploaded) and photos whose picture lives only in cloud storage (normal, expected, not this device's job, and never to be presented as a shortfall). Never fold these back into one ratio.


@ S551 on a small screen the bar keeps the CONTROLS, not the decoration (Mark, verbatim: "I really only just need the functional buttons outside, i.e. back, arencon logo, undo/redo, and menu. Others take lesser priority"). Before this the logo held roughly a third of a phone-width bar and never yielded any of it, so undo and redo — the controls an inspector reaches for most on site — were the FIRST things pushed into the drawer, where they rendered as an unlabelled floating pair. Undo/redo are now exemptUntilLast, the protection Sign Out already had; the storage meter folds first (foldRank 5) because it is a number you look at once a month. Below 520px the logo scales to 26px; below 480px the title is hidden and its 60px reservation goes with it. NOTE: hiding the title DELIBERATELY OVERRIDES the S448/S488 lock that the title truncates but is never hidden — Mark's call, because "Diese..." says nothing the project bar underneath does not, and the room it costs is the room undo and redo need. The width classes come from the BAR's own width, never a media query: this header lives in a shadow root inside tools that are also embedded, so the viewport width is not the width that matters. Fold ranks were applied to the Diesel and Electric configs only; FRT's own block is Lane A's.


@ S552 Electric joins the shared photo store — it was the LAST tool whose photos could not be rescued at all. A picture is stripped from a report when it uploads, so without a local image file there is nothing anywhere to restore from if the stored copy goes missing; Electric kept none. It now runs the SAME engine as Diesel (lib/data/photoStore.js) and supplies only what Electric knows: its database (ARENCON_ELECTRIC v4, +photoBlobs), how to list its photos (_collectAllPhotos, mapped to .photo), and where it keeps the picture (.d). NO Diesel code was copied across — a second implementation is the matching-copy trap and the copy is always the one that stops being maintained. The sweep is called from saveState and is GUARDED (typeof check) so a save can never fail because a shared module did not load. Photo Store Check added to Electric's More menu for the same reason as Diesel: the field tablets are the installed app with no console, and the store is per-device.
_elecPhotoStoreCheck
_electricLocalBytes


@ S553 ONE resolver, and a per-photo retirement test. Retiring the inline picture from a Diesel report is a REFACTOR, not a switch: about forty places read those bytes directly and SYNCHRONOUSLY (baking a markup layer, building the PDF, uploading, making a backup), while the device's own file is an ASYNCHRONOUS read. Flip retirement on before those places can wait and they get an empty string — a blank page in a client's report, a markup baked onto nothing. resolveSrc is the single door they must all come through; its order is deliberate (inline while it exists, then THIS DEVICE'S file, then cloud — the local file is preferred over cloud because it works with no signal, which is the case the store exists for). Object URLs are minted once per photo and cached, or a 200-photo gallery mints 200 more on every repaint; release() must be called when a report closes or they pin every image in memory. retirable() gates PER PHOTO and never per device: the file must be held here AND the upload confirmed, both, because either alone is a single point of failure. Note the cloud side has ALWAYS been retired this way (_keepD drops the picture once r2Status==='uploaded'), which is why no version floor and no fleet-wide check is needed for the local side — the inline copy never crosses devices. _localSrc is a live object URL and must NEVER be persisted; _stripPhotos rebuilds photo records field-by-field, which is what keeps it out.
resolveSrc
retirable


@ S553b/S554 the inline photo copy is RETIRED, per photo, on the save path. A Diesel report carried every photo twice on the device: a real image file in the store, and the same picture as text inside the report itself — copied in full every 15 seconds by the autosave. retirePass drops the second one, but ONLY after this device's own file has been read back and attached to the photo as a live source, so at no instant is a photo without a way to be drawn. That ORDERING IS THE ENTIRE SAFETY ARGUMENT — never reverse it. Gated per photo (upload confirmed AND file held here), 25 per pass because it runs behind someone typing, and it only mutates the in-memory report so the smaller payload lands naturally on the next save. DELIBERATELY SKIPPED: annotated photos, whose inline copy is the BAKED MARKED IMAGE and not a duplicate of the original, and backup records (_isOrigBackup) which exist to be the last clean copy — both still have markup paths reading the bytes directly and must not be retired until those are routed through resolveSrc. An object URL is far cheaper than what it replaces: the inline copy is text in the JS heap re-serialised on every save, the URL points at a file the browser keeps on disk. _localSrc must NEVER be persisted; collectState and _stripPhotos both rebuild photo records field-by-field, which is what keeps it out.
retirePass


@ S555 the change journal — stage ONE of change-based sync, and RECORDS ONLY. Today a device sends its entire picture of the report and hopes. When Franz's report was wiped on 7155.40 the save that did it looked exactly like every other save from the outside: a complete document written over a complete document, with nothing recording that this one arrived carrying 0 photos where the last carried 223. The evidence had to be dug out of history rows afterwards and six flow readings were never recovered. The journal writes one small entry per save — what each collection went from and to, who was at the device, which build — and flags anything that lost a third or more at once, which is the shape a wipe makes. IT MUST NOT block or alter a save, travel to the server, or feed the merge until it has been watched against real inspections: a guard firing on a rule nobody has seen in the field is how an inspector ends up unable to save on site. It never deep-compares the report and never touches photo bytes — it counts, because it runs on every autosave behind someone typing, and every path is wrapped so a journal failure can never cost a save. `collections` is where each tool says what its own report is made of, in PLAIN LANGUAGE, because these entries are read by a person on a tablet. Surfaced as "Recent Saves" in the More menu for the same reason as the Photo Store Check: the field tablets have no console.
createChangeJournal
_dslSaveLog


@ S556 Electric's photos become rescuable. S552 gave Electric its own copy of every photo it takes, but NOTHING READ IT — coverage that looks done and is not, the same trap Diesel sat in before S544, and made worse by the Photo Store Check reporting photos as held while nothing could actually put one back. Electric now runs the SAME rescue pass as Diesel: a photo whose stored file has gone missing is re-uploaded from this device's own copy, one with no image anywhere is reported rather than left as a dead tile. ONLY the rescue is on — Electric keeps its own upload queue (R2Photos), nothing is ever enqueued into this engine, and its uploader and pull-time re-injection stay dormant because those walk FRT's report shape. `projectId` is REQUIRED here as it is for Diesel: Electric's report carries no id of its own and the Hub folder is the truth; without it verifyR2Keys quits on its first line. Electric's cloud layer has no post-load callback, so the pass waits for the report to actually be up and runs late on purpose — an inspector opening a report must never be made to wait on a network sweep.
_elecRescue


@ S558 checklist items are reassign destinations (Mark, for Franz's mis-filed photos). Diesel's reassign has been a correct MOVE since S533 — same record object relocated, detach by identity, refuses to place if the detach did not find it — but the destination list offered record boxes, flow tests and deficiencies and NOT checklist items, which is the commonest mis-file on a commissioning job: a photo taken against the wrong checklist line, or dropped into Site Photos when it belongs to one, could be moved anywhere except where it belongs. Labels carry the item number and de-duplicate if two resolve to the same visible text, so two lines can never be confused in the picker. The placement branch creates the item's photo array when it has never held one — by that point the detach has already succeeded and the photo is in NO list, so it must land or it is lost. NOTE FOR ANY FUTURE SESSION: a duplicate _pgReassignSelected was very nearly shipped in S557 from a stale roadmap note claiming reassign was outstanding. It would have overridden the working S533 implementation silently, and the symbol gate CANNOT catch that — nothing disappears when a second definition is added. Read the live file before building anything the roadmap calls missing.
_CLDEST


@ S559 part06.js is FOUR FILES (part06.js + part06b/c/d.js). It had reached 9,601 lines and 547KB — five times the next largest file in the tool and the hardest thing in the codebase to work in safely. Cut at top-level boundaries ONLY; nothing renamed, exported, wrapped or moved between pieces. THEY ARE ONE SCRIPT IN FOUR PIECES, NOT FOUR MODULES: plain scripts sharing one global scope, loaded in order b then c then d immediately after part06.js. LOAD ORDER IN diesel-app/index.html IS LOAD-BEARING — change it and functions vanish for whoever runs first. HOW IT WAS PROVEN, and how any future re-cut must be: (1) joining the four files back together reproduces the original BYTE FOR BYTE, (2) each piece passes node --check standalone, (3) the gate's OWN js_symbols extractor run across all four files together shows 1381 symbols before and 1381 after, 0 lost, 40 protected symbols present and 0 lost. THE ONE-FILE GATE BLOCKED THIS PUSH on 29 protected symbols and was right to by its own rules — it compares one file against one file and cannot express a move between siblings. That block was overridden on the strength of proof (1)+(2)+(3) and disclosed to Mark, NOT worked around silently. Anyone splitting a file again must produce the same three proofs; --kill alone is not sufficient and never was.

@ S556 the integrity check is ON SCREEN (More menu), not a console command. The field tablets are the installed Android app: no console, no address bar. The check answers "are this report's photos and records intact on THIS device" — per-device by nature, so it must run where reports are made. Read-only: reports, never repairs. The five dim-on-desktop controls (remove/dup deficiency, tag removers, sr-pin) rest at full opacity on coarse pointers.
mobile-integrity-btn
_frtIntegrityPanel


@ S560 annotated photos and backup records join retirement, and the reload gap is closed. hydratePass fixes a real gap in S554: _localSrc was only set at the MOMENT of retirement, so after an app restart a retired photo had an empty picture field and nothing pointing at the file on disk — offline, a broken tile for a photo the device holds. Hydration now runs post-load (part02 kick) and at the top of every retire pass, so retirement can never outrun reattachment. The two S554 skips were lifted DELIBERATELY and differently: backup records (_isOrigBackup) retire under the standard gates because their inline copy is the same clean pixels the confirmed /original/ upload holds — a true duplicate; annotated photos retire ONLY when their stroke objects (p.mk) exist, because with strokes the marked image is a COMPUTATION (clean original + strokes recomposites anywhere) while a legacy baked-only photo's inline copy can be the LAST copy of the markup in existence — cloud /original/ is clean — and is kept unconditionally. Never collapse these two rules into one. The bake loader (_dslLoadBakeImage, part06c) accepts blob: object URLs and the backup's _localSrc as local sources, so re-baking works offline on retired records.
hydratePass


@ S561 the R2 orphan report is on screen (Cloud Storage Check, More menu). The report function itself (_dieselR2OrphanReport, part06d) is UNTOUCHED — it already returned {bucket, orphans, missing} and just had no face except the console, which the field tablets do not have. Read-only like the other panels. The PURGE stays a console operation DELIBERATELY: deleting cloud objects is not reversible and belongs at a desk with the full list in view, not behind a tap on site — do not add a purge button to this panel. Missing files are described with their remedy (the rescue pass re-uploads from a device still holding the picture); stranded files are described as a storage cost, not a correctness problem.
_dslOrphanPanel

@ S557 dimension loupe + adjust-then-place (Mark-specified after team feedback "insanely hard on touch"). The loupe magnifies what the fingertip covers, during both the measuring drag and endpoint adjustment, coarse pointers only. The adjust stage makes the lift non-final: endpoints stay grabbable, pan/zoom allowed, nothing commits until the green check — also the only way to measure a span longer than one drag. Removal puts back blind aiming and lift-is-final.
_drawLoupe
_dimEnterAdjust
_dimAdjustFinish


@ S562 the three small carried items, closed. (1) PLACARD SCAN AND THE PUMP BOX: the scan looked ONLY in its own Placard box and answered "Capture a placard photo first" while the inspector's placard shot sat in plain sight in the Pump box — the commonest mis-file on site, and silently ignoring a visible photo reads as the tool being broken. It now falls back to the MOST RECENT non-deleted Pump-box photo and SAYS SO in a toast, so a wrong guess is visible rather than silent; the existing preview/confirm still gates every value before anything is written, so scanning a photo of the pump body costs one worker call and an obviously-wrong preview, never a wrong report. The tab's own placard kind still wins whenever it exists (S338 per-pump scoping unchanged). Photo LOSS on the scan path is a SEPARATE scoped defect (SCOPE_PLACARD_SCAN_PHOTO_LOSS.md, Option C investigate-first, Mark present) and was deliberately NOT touched here. (2)+(3) ONE MEANING PER GLYPH in the flow-equipment photo viewer: the per-photo button was a ✕ titled "Remove" in the same viewer as the header's ✕ that means CLOSE — two identical glyphs, opposite meanings, one destructive — and "Remove" understated a project-wide soft delete. It is now the bin glyph titled "Delete photo (moves to Recently Deleted)", matching the lightbox and the rest of the toolkit: bin deletes, ✕ closes, ⇄ moves (its title now says it keeps the photo). Confirm modals unchanged and verified present on every delete path including the lightbox's.


@ S563 photos taken before the project folder was known finally get uploaded — and the placard photo-loss scope is CLOSED. INVESTIGATION RESULT, recorded so nobody re-opens it: the loss described in SCOPE_PLACARD_SCAN_PHOTO_LOSS.md ("add a placard photo, scan, photo gone, not in Recently Deleted") was ALREADY FIXED at S488. ArcPhoto.mint (lib/data/photoMint.js) now enqueues the upload and calls saveState on the next macrotask, so a photo is durable AT BIRTH and no call site can forget — the ~30 call sites that did photos.push(mint(...)) and nothing else were the whole mechanism, and a refresh or a cloud merge in that window destroyed the photo. Verified by running the real mint: photo in the array at save time, one saveState, one upload queued. The scope doc is STALE — do not chase model/scan code for this again. WHAT S488 COULD NOT COVER, and this fixes: _r2EnqueuePhoto returns early when _r2FolderId is null, so a photo taken before Hub init completes (or in standalone, before the report is later opened from the Hub) is saved locally and then uploaded by NOTHING. It is not lost, but it lives on ONE device forever, and the rescue pass cannot see it either — that pass verifies existing cloud keys, and a photo with no key is invisible to it. _dslUploadUnsentPhotos is the manual Re-upload All narrowed to exactly those photos and run automatically once the folder is known. Deliberately narrow, keep it that way: no cloud key at all, still carries its picture, not deleted. A retired photo can never be in this set — it has no bytes here AND by definition a confirmed upload.
_dslUploadUnsentPhotos
@ S568-S571 pin integrity + intent gate (Mark). A pin drag ANYWHERE requires the 500ms armed hold (blue glow) — S331w's "one finger on a pin is ALWAYS a drag" caused every recorded teleport (17 events, 5 inspectors) and must never return. Off-sheet commits are REFUSED and restored, never clamped onto the edge; a pinch mid-drag KEEPS a deliberate placement (validated) — it never discards it and never drags the pin. Every pin write logs to window._frtPinWriteLog (on-tablet, no console needed). Tiled sheets: pins x1.2 at tile level L3, x1.3 at L4+ (TiledPdf.getLevel).
_pinDragCapture
_pinDragRestore
_pinCommit
_pinViewerWriteLog
_pinDragLastFx
_frtPinWriteLog
getLevel
_liveLevel
@ S567 thread amendability rule (Mark-approved fix). _threadEntryAmendable is THE model-side answer editThreadEntry/removeThreadEntry ask; it mirrors crbThread _actionRow (amendable = not printed-in-issued-report, or deliberately unlocked). Model and buttons must move TOGETHER or issued records can be silently altered (looser) or editing silently dies again (stricter — broken S500-S566).
_threadEntryAmendable
@ S566 activity modal module (deficiency split stage 1). frt/js/ui/activityModal.js owns _showActivityModal; deficiencies.js imports it back. Verbatim move, reassembly-proved.
_showActivityModal
activityModal

@ S587 on-tablet Pin Write Log (Mark: the crew hits pin problems in the field and only a PC console could read the evidence). A read-only ... menu panel showing every accepted/refused/ignored/disarmed pin write this session, with Copy report. Field tablets run the Android TWA with no editable address bar, so a menu row is the ONLY way in - never a URL param. Removal returns the firm to month-later forensic digs.
_showPinWriteLog

@ S608 (Lane A) FRT sync self-report + unhangable boot (Mark's item 3: "FRT adopts the shared sync engine"). The listening loop now records WHY on every beat (_frtTickDiag ring diary, readable on-tablet from the cloud-dot popup — field tablets have no console), a watchdog releases a hung pull instead of going deaf for the session, network calls are time-bound, unsent work pushes BEFORE any pull can touch it, and the S605 stats-based re-arm keeps this device's newer entries from losing to the push dedupe forever. Boot: every network step is time-bound (_bootStep), a stalled sign-in works from the device copy instead of bouncing to a login page the connection cannot load, a timed-out boot pull is NEVER treated as an empty cloud (no fabricated blank reports — _bootPullRetry keeps asking), and a 25s watchdog forces the local render up rather than a page that never finishes. Notable outcomes go off-device to sync_diag. Harnesses frt/tests/sim/{tickhealth,bootstall,stalemate}.mjs read the shipped code and fail if any of this regresses. Removal returns FRT to the four-indistinguishable-faults era that cost Diesel nineteen sessions.
_frtHeartbeatTick
_frtTickDiag
_frtTickLog
_frtSyncDiag
_frtWithTimeout
_bootStep
_bootPullRetry
_bootPullTimedOut
_frtBootLog

@ S608 (Lane A) FRT typed photo fields in the shared merge (the S605 treatment; Mark's item 3). FRT photo dirtiness is judged only on what a person can change — caption, rotation, delete markers, and the never-bake markup vectors — never on the device-only binary the cloud strips, which had every photo reading "locally edited" forever so another tablet's caption edit or delete could never land. _lwwStripFields serializes non-scalar typed fields canonically so a MOVED mark counts as an edit. Deficiency/observation entries deliberately left untyped until Lane C closes the deficiency-propagation investigation (see frt/tests/sim/deficsync.mjs). _lwwReplay/__lwwTestHook let the harness exercise the SHIPPED merge with no I/O.
_lwwReplay
__lwwTestHook

@ S608 (Lane A) FRT live update on the shared engine (Mark's item 4). initLiveUpdate replaces the S207 "Update ready / Refresh / Not now" banner + bottom-right indicator, which are REMOVED at Mark's explicit direction ("its old S207 banner + indicator must be removed") — do NOT restore _showUpdateReadyBanner/_showUpdateReadyIndicator/_doUpdateReload from history; the engine owns listening, staging, safe moments and the swap, and FRT supplies only flush (Model.saveNow — the S162 primitive, preserved), isBusy (viewer open / photos in flight) and capture (tab + scroll). The tab now lives in the address bar (?tab=, written by switchTab, read by _restoreView), so ANY reload — refresh, update swap, crash — returns the inspector to the tab they were on instead of Project Info. TWA users cannot edit URLs; the app writing its own state is the sanctioned path.
initLiveUpdate
restoreAfterUpdate
_restoreView

@ S613 (Lane A) emptiness ignores values nobody typed — the FRT ghost-row fix. FRT stamps defaults onto every row at load (contractors get a palette colour, observations get priority/tradeSource/repeatCount, deficiencies get a migration flag), and the merge's emptiness test read those machine-written values as an inspector's work. A genuinely blank row could therefore never be recognised as empty: it survived absence-never-deletes and unioned into everyone's copy of the report — the same ghost-row machine as Diesel's 21→32, with FRT's own defaults as the cause. _lwwItemEmpty now takes a per-family `defaults` map declared in _LWW_SPECS and VALUE-MATCHES it: `priority` is ignorable only while it still reads 'high'; the moment an inspector sets 'low' the row is content again. '*' means machine-chosen and never typed. Tools that declare nothing are untouched — Diesel's behaviour is unchanged (tools/sim/converge.mjs 50/16 before and after). THE RISK THIS CARRIES, and the guard on it: widening "empty" buys ghost removal at the risk of discarding an inspector's in-progress row, which would be far worse than the bug. frt/tests/sim/converge.mjs check R builds rows through Model's OWN creation calls and asserts they survive a pull that has never seen them; it was proved to have teeth by a negative control (an over-aggressive defaults list makes all three R checks FAIL). Never widen a `defaults` map without re-running that control. Removal returns FRT to un-droppable blank rows spreading across every device.
_lwwItemEmpty

@ S622 stamp-conservation sync engine (Mark, 06 Aug 2026: "root cause, permanent, commercial grade" — the scalar/fieldMap loss arc, field-verified S622f)
_lwwRevertDirtyKept
_resolveSpecConflicts
_pushRun

@ S622g pump-type hard lock + 3pt/7pt colour sectioning (Mark, standing queue item; Level 2 approved 06 Aug 2026)
_ttHasPerfData
_ttApplyLock
_ttCommit
.tt-locked

@ S627 (Lane A, Mark on-device 07 Aug 2026) THREE FIXES BEHIND ONE COMPLAINT — "the update button keeps coming back, and I don't see any inspector ring". (1) THE DRAWING-VIEWER BUSY GUARD HAD NEVER FIRED. isBusy/busyReason tested `dv.offsetParent !== null`, and offsetParent is ALWAYS null for a position:fixed element — which .drawing-viewer-overlay is (frt.css inset:0). The check read "viewer not visible" with the viewer filling the screen, so a tap on the update pill reloaded straight out of an open drawing, skipping the markup save that runs on viewer CLOSE — the S528 loss family, live and unguarded since S622, and the reason Mark never saw "close the drawing to apply". _dvOnScreen asks getComputedStyle + a painted rect instead of a layout side-effect a fixed element does not have; do NOT revert to offsetParent, and do NOT substitute a class check (the overlay is shown by INLINE style). (2) ONE SERVICE WORKER SERVES TEN TOOLS, so every lane's push told every FRT tablet a new build was waiting — four pump-lane pushes on 06 Aug produced four pills on devices running identical FRT code, which is how a crew learns to ignore the one pill that matters. ownBuildChanged() (engine, optional, defaults to showing the pill) + _frtOwnBuildChanged (reads FRT_BUILD out of the incoming app.js so there is no second stamp to drift) suppress only the PILL; the build still stages and still applies at the next safe moment, because shared lib/ modules are real FRT code and must land. Any failure to decide falls through to showing the pill — an extra pill is a nuisance, a missed update is a stale tool in a pump room. (3) INSPECTOR RINGS WERE INVISIBLE ON REAL WORK: the rule was "two or more IDENTIFIED people", and every ARENCON report predating spring 2026 carries pins with no author at all (1490.04: five of six). One identified inspector is now enough, rows carry their pin count, and an Unattributed line states how many pins can never carry a ring — a feature that hides itself cannot be told apart from a broken one. A pin with no author still gets NO ring: guessing who placed it would put a wrong name in a client report. (4) FRT never wrote data-theme on the root element, so every shared dialog and the whole Help panel rendered their LIGHT skin on a night-mode tool with the dark skin unreachable — the fault the pump tool fixed at S497 and the Hub carried to S622. The body class stays authoritative for FRT's own stylesheet (which has no data-theme rules); the attribute is the shared layer's copy of the same truth.
_dvOnScreen
_frtOwnBuildChanged
ownBuildChanged
_unattributedPins

@ S623 MILLISECOND CLOCK (Mark, 07 Aug 2026 — "two values typed a second apart on two devices and the earlier one won"). THREE FAULTS, one symptom. (1) THE FLOOR PINNED A SKEWED MINT. The boot pull ran through Auth.request, which never feeds the clock learner, so the FIRST value typed after opening was always stamped on the raw device clock — and the monotonic floor then held that stamp, publishing floor+1ms for as long as the skew lasted. A tablet 8s fast beat everything anyone else typed for the next 8s. This is why the S622i gross-skew fix was inert: it learned the right offset and was overruled by its own guard. The boot pull now goes through _rawFetch so the clock is known BEFORE anything can be minted, and _nowSync drops a floor standing >100ms above the corrected clock (a floor minted on a clock we have since learned was wrong). Do NOT revert the boot pull to Auth.request, and do NOT restore an absolute floor. (2) The Date header is 1s resolution, which forced a ±1500ms dead band and let every sub-second skew through; it is now a FALLBACK ONLY, superseded the moment a write lands. (3) tool_data has a BEFORE UPDATE trigger (NEW.updated_at = now()) and both write paths already ask for the row back, so every save returns a Postgres timestamp at microsecond resolution — verified live 07 Aug. _learnServerStamp pairs it with the local send/receive times, keeps the LEAST-DELAYED sample (NTP's minimum filter, because a median averages good samples with congested ones), applies a correction only when it exceeds half the round trip, and SHRINKS it by that uncertainty so a correction can never overshoot into artificial future — overshoot is what defeated the S622i centring attempt, and under-correcting only costs a device a race it won by less than its own network latency. RESIDUAL, stated rather than hidden: the correction is only as tight as the round trip (~100-200ms in the field), and stamps published before a correction stay in the cloud. THE PROOF: tools/sim/clockskew.mjs — the first harness giving each device its own clock AND the mock server a real one; 7 checks including two negative controls (agreeing clocks must not acquire artificial skew; a device must still overwrite its own pre-correction value) and a SELF-VALIDITY assertion that refuses to score a race whose real gap exceeded the skew under test. That assertion exists because this probe went toothless once mid-session: modelling the boot pull stretched the gap past the skew and the 900ms check started passing on the broken engine — the same way three previous scalar fixes shipped green and failed in the field. Never delete the validity assertion.
_learnServerStamp
_learnFromRows
_applyOffset
_parseSvrIso
_nowSync

@ S624 HEARTBEAT LIVENESS (Mark, 07 Aug 2026 — device and-ceerf7 stopped emitting to sync_diag entirely at 21:22Z while the phone and PC kept syncing; a typed value stranded; only a refresh recovered). THE GAG: _syncHeartbeat opened with `if(_syncLock || _heartbeatRunning) return;`, raised the flag, awaited CloudSync.heartbeatTick() and lowered it after. A REJECTED tick was always survivable — the catch ran and the flag came down. A tick that NEVER SETTLES was not: a hung fetch (WiFi handover, captive portal, dying pump-room signal) neither resolves nor rejects, so the flag stayed raised forever, the 15s timer kept firing, and every beat returned AT THE GATE — before any logging, which is why a device in this state cannot even report its own condition. The engine's TICK_WATCHDOG_MS is no help whatsoever: it guards a flag INSIDE heartbeatTick, and the outer gate prevents heartbeatTick from ever being called. FOUR LAYERS, because any one alone leaves a hole: (1) HB_TICK_TIMEOUT_MS races the await and the flag is released in a FINALLY, never on the happy path only — a throw between raise and lower was the same permanent gag by another route; (2) HB_GAG_WATCHDOG_MS watchdogs the FLAG ITSELF at the gate and force-releases with outcome 'heartbeat-gag-released', so a failure nobody imagined still breaks out inside a minute; (3) _hbCheckLiveness on tab-visible/focus/online restarts a timer that STOPPED FIRING — a timeout cannot rescue a beat Android froze while backgrounded and never resumed; (4) visible staleness, because a stalled device that looks healthy is the real hazard. THE STALENESS THRESHOLD IS DELIBERATELY NOT A MINUTE COUNT: _hbStaleThresholdMs reads the scheduler's own declared interval (ArcSyncCadence.desiredIntervalMs) and flags three intended checks missed, with a floor — so it follows the cadence when it is retuned and when Realtime makes scheduled beats rare. Never replace it with a constant. NOTE THE MOBILE TRAP: diesel-01.css S267 force-hides #last-sync-text on narrow screens, so on the TABLET — the device that actually stalls — the warning would never have been seen; the .is-stale rule overrides that hide and must not be removed. Amber not red: the data is safe, it just has not travelled, and reserving red for real loss keeps red meaningful. Ported verbatim to Electric, which carried the identical shape and therefore the identical fault. Liveness only — nothing here touches stamps, merge outcomes, the collision door or the push dedupe. THE PROOF: tools/sim/heartbeatgag.mjs extracts the heartbeat region VERBATIM from the shipped file and runs it against a promise that never settles; the fail-first arm extracts the same region from the PRE-FIX commit, so BOTH arms run real shipped bytes rather than a transcription. Pre-fix 2/10, fixed 10/10, including a negative control that three healthy beats emit no liveness telemetry at all. KNOWN, DECLARED: blur/look-now pulls do NOT share this gate (they call the engine directly with their own time-based throttle) — they hang on the same dead network but never gag permanently; and _syncLock is declared and read at the gate but never set anywhere in either tool.
HB_TICK_TIMEOUT_MS
HB_GAG_WATCHDOG_MS
HB_STALE_FLOOR_MS
_hbDiag
_hbStaleThresholdMs
_hbIsStale
_hbStaleMinutes
_hbCheckLiveness
_hbWireLiveness
reportDiag

@ S625 STAMP PURITY + PERSISTENT LEDGER (Mark, 08 Aug: "stop patching and fix it at root"). ONE LAW: A VALUE NEVER WEARS ANOTHER VALUE'S STAMP. sync_diag 05:26:39Z proved the violation — IP's 528 carried the millisecond-exact stamp of AD's 25, then olderWon:true, then a permanent split refresh could not heal. Mechanism: merge3's _fts max-merge dresses values in the other side's stamps (its own comment admits it), and BOTH 412-door ledger assignments took that dressed document as the stamp ledger with no value-alignment — the pull path had the honesty revert, the 412 path did not. A narrow two-person race almost always 412s on the loser's side, which is exactly "narrow gaps often fail, wide gaps work". _alignSpecStamps re-asserts the law over every spec scalar and fieldMap key after ANY 412 merge: merged value == mine → my recorded stamp travels; == theirs → theirs; == neither → 0, "entry time awaits its mint" — an honest zero loses until the next save mints truth, a borrowed stamp wins races it has no right to. Times are MOVED, never minted at merge — the dirty?now fabrication has been killed four times and must never return. Equal-stamp wreckage (each device lawfully preferring its own copy, forever) heals by deterministic tie-break on canonical value order — identical on every device, NOT local-wins, because local-wins IS the deadlock — and a tie-kept local counts keptLocalNewer so the heal is pushed. PERSISTENT LEDGER (OPEN 3 root): _lastStampedLocal died with the process, so a reopened app held the typed value with no record of WHEN — an unstamped value loses every time, which is why the offline revert was reliable, not intermittent. The ledger now rides in syncMeta beside the snapshot with the clock offset, and stampLocal persists it the moment an offline stamp pins — closed exactly where the file declared it queued. B2: an offline heartbeat return is NOT cloud contact; counting it made a stalled device refresh its own freshness pill every 15s in airplane mode. DECLARED OPEN, not claimed: after a relaunch the healed value reaches the cloud on the first save (any interaction; typing-saves makes that ~1s) — a zero-interaction boot flush is follow-up, proven missing by tools/sim/offlineboot.mjs check 3, and the stamppurity poison-heal scenario exercises the merge tie-break but cannot drive the facade push re-arm.
_alignSpecStamps
_lastPersistPid
@ S628 (Lane A, Mark 07 Aug 2026) INSPECTOR IDENTITY COLOUR — EACH PERSON PICKS THEIR OWN, FIRM-WIDE. _inspectorColor hashes a user id into a fixed 12-slot list; stable, but a GUESS, and with 17 staff over 12 slots it was guessing wrong — eleven of seventeen shared a ring colour with somebody else (pale lime: Alex/Shaun/Thomas; light lime: Donovan/Jim/Nasim/William; cyan: Anna/Franz; amber: Ian/Matt). On a job with two of them pinning, the ring asserted that two different people were one person: a signal confidently stating something false, which is worse than no signal. profiles.ring_color (migration s627_inspector_ring_color, format-checked #RRGGBB, nullable) now holds the person's own choice and follows them to every project, tool and device. inspectorColorFor() is THE resolver — chosen colour first, id-hash only for someone who has never picked — and every colour site routes through it (pin ring, Layers panel swatch, resolveInspector, setInspectorEntry). NEVER call _inspectorColor directly from a rendering path again: that skips the choice and a chosen colour silently never reaches the drawing, which is precisely the S83 failure shape (engine complete, tap never connected). The 'inspectors' listener in viewer.js is load-bearing, not a nicety: profiles resolve AFTER first paint, so without a repaint every ring stays on the fallback colour for the whole session and the picker looks broken. Palette: 48 colours, generated not hand-typed, each >=26 dE from every pin body colour, contractor colour, legacy contractor colour and both sheet backgrounds, and >=21.8 dE from each other — 21.8 being the separation the shipped 12-colour palette already meets, chosen over an invented number. 48 is the CEILING at that separation (verified by a 43k-candidate packing search over the widened L* 58-90 band); more colours require either closer colours or a second ring signal, and Mark rejected BOTH — dashed rings are REJECTED, do not re-propose.
inspectorColorFor
_inspectorChosen

@ S628b (Lane A, Mark field-verified 08 Aug 2026: "I'm still pink") THE CHOSEN COLOUR COULD NOT REACH THE PERSON WHO CHOSE IT. setInspectorEntry seeds a cache row at sign-in (S143) so your own name shows instantly with no round trip — but that row is NAME-ONLY, has never seen the profile record, and therefore carries the id-hash fallback colour. primeInspectors' filter treated "in cache" as "resolved" and never fetched the real record, so ring_color reached everyone EXCEPT the signed-in user. Exactly the reported shape: colleagues correct, self still pink. _inspectorPartial marks locally-seeded rows as placeholders so the fetch still runs and clears on the server's answer. NEVER let a locally-seeded cache row suppress the authoritative fetch — a placeholder is not an answer. Also: the Layers popover carried an inline min-width:180px from before it listed people; contractor and inspector rows wrapped to two lines and wasted the panel. Widened to 262-340px with single-line rows and ellipsis, 44px targets kept.
_inspectorPartial

@ S628c (Lane A, Mark field-verified 08 Aug 2026, D1 FAIL) PHOTO MARKUP DID NOT SURVIVE THE TAB CLOSING — live data loss on the surface inspectors use most. Until a save runs, strokes exist ONLY inside the markup engine; _saveMarkup() is the sole path onto the record and fires only on close, Save, or navigating photos. Nothing ran it when the page went away. The DRAWING viewer has had an unload flush since S125; the photo lightbox never got one. _saveMarkup CANNOT be called from an unload handler — it awaits cleanBlob(), which can take seconds on a tablet, and a closing page does not wait for promises; calling it there looks like a fix and reliably loses the race. _flushMarkupForUnload instead writes the two fields a real save writes (_markupStrokes, _mkFrame) SYNCHRONOUSLY and marks dirty, plus a localStorage rescue (RESCUE_KEY, keyed by photo id so it can never land on the wrong photo) because the dirty flag schedules an async write that may also not finish. _restoreMarkupRescue only FILLS A GAP on boot — never overwrites strokes already on the record. Armed on pagehide AND visibilitychange AND beforeunload: pagehide is the one that fires on Android tab close and app backgrounding, beforeunload alone is not reliable there. In app.js it MUST be reached via window._frtLightbox — app.js does not import the module, so a bare Lightbox reference throws ReferenceError, the catch swallows it, and the fix silently does nothing (caught during this build).
_flushMarkupForUnload
_restoreMarkupRescue
_clearMarkupRescue
flushForUnload
restoreRescue
RESCUE_KEY

@ S628e (Lane A, Mark field-verified 08 Aug 2026) THREE FIXES. (1) CONTRACTOR COLOUR MOVES TO THE PIN TIP. Mark asked three times; I cited the S328 "whole teardrop recolour, LOCKED" record twice before doing it. Once was defensible, twice was not — it is his tool and the record was wrong. THE S328 SPEC IS SUPERSEDED: contractor colour paints the TIP ONLY, body keeps its priority/status fill, ring keeps the inspector colour. Do NOT "restore" whole-body recolour on the grounds that an older document says so. Rationale that also makes it correct on the merits: a pin carries three facts — who placed it (ring), how bad it is (body), whose trade it is (tip) — and whole-body recolour swallowed two to show one, which became obvious once rings were on by default. The tip wedge is CLIPPED to the teardrop (to 0.88 when a ring is drawn) so it can never spill past the silhouette, and sits below the white number disc so it never fights the number. The glow filter now keys off the priority colour, not the contractor colour. The DIM on non-matching pins is unchanged. (2) THE CONFLICT MODAL WAS ASKING ABOUT ITS OWN PLUMBING: Mark got "3 items need your decision" whose three items were _tab, _wroteAt and modified — a browser tab id and two self-stamped timestamps — and correctly refused to answer. _isHousekeepingPath drops underscore-prefixed leaves and known bookkeeping names BEFORE the empty check, so an all-housekeeping clash resolves with NO modal at all. This does not skip them: mergeApplyResolutions only overrides paths it is given, so the merge's own rule stands. Never surface internal fields as a user decision — it teaches people the modal is noise, and then they click through the one that matters. (3) LAYERS POPOVER WIDTH, MOBILE TOO: S628b widened only the desktop rule; inside the mobile media query `#dv-layers-menu[style*="block"]{min-width:180px}` is MORE specific than the id alone, so on a tablet — the device this list is actually read on — names kept wrapping. Matched the attribute selector rather than editing the shared S79 positioning block.
_isHousekeepingPath
_HOUSEKEEPING_LEAVES
ctrTip
_userRingColour

@ S628f (Lane B file, Lane A build — Mark: "everyone pick their own colour, in account settings") THE COLOUR PICKER. Lives in the Hub profile panel. 48 swatches, GENERATED not hand-typed: every one clears >=26 dE from all five pin body colours, every contractor colour and both light and dark drawing sheets, and >=21.8 dE from every other swatch — the same separation the toolkit's original inspector palette already met. 48 is the CEILING under those two constraints, proven by a 43k-candidate packing search; it is not a round number. DO NOT add colours by relaxing either floor: closer-to-a-pin-colour means an unreadable ring on a real drawing, closer-to-each-other means two inspectors who cannot be told apart, and Mark rejected both. A colour taken by a colleague shows their initials and cannot be selected — one colour, one person, firm-wide. Anyone who has never picked keeps their auto-assigned colour, so nothing goes blank and nobody is forced. Preview shows the badge plus the real teardrop on a light sheet AND a dark sheet, because a ring must survive both. Save writes profiles.ring_color only; the S628b privilege guard rejects role and is_active from a browser but never this, so a failure here is genuine and is shown, never swallowed. Board repaints on save — a settings change you cannot see is one people repeat. NOTE the profile panel body is a backslash-continued string literal: every inserted line needs exactly ONE trailing backslash (a doubled one silently terminates the markup) — caught during this build.
_RING_PALETTE
_ringOwnerMap
_ringSwatchGrid
_ringPreview
_ringRefresh
_ringSave
_ringPending
_ringInitials
ring-host
ring-save
.ring-sw

@ S626b OFFLINE EDIT SURVIVES THE APP CLOSING (Mark, 08 Aug: "fix it, do not waste more of my time" — the 77-reverts-to-200 case, still failing after S625 and S625b). FOUR LAYERS ON THE BOOT PATH, none visible from the screen, each hidden behind the last. (1) The ledger persisted under a project id the engine only learned from a completed save, so an offline edit in a fresh session persisted nothing — every engine door (loadIDBSnapshot, pull, push) now captures it. (2) The boot pull replaced the restored ledger with a cloud-derived document moments later, erasing the restored entry times — _reassertBootLedger re-asserts them per key on the APPLY path, only where the disk stamp is strictly newer; times are moved, never minted. (3) THE ONE THAT MATTERED: the restore sat BELOW the S132 blank-snapshot guard, which returns early — and an offline edit's snapshot is deliberately stripped of the unpushed value by the S622 honesty revert, so "blank snapshot" and "has unsent work" are NOT opposites, they co-occur exactly in the case this protects. The code never reached the restore at all, which is why S625b appeared to change nothing. The ledger restore is now hoisted ABOVE that guard, unconditionally: it is read-only evidence about WHEN this device typed things, it renders nothing and is never a merge base, so the guard's concern does not apply to it. NEVER move it back below. (4) Even restored, the device did not know it was ahead: the boot pull skips the per-item stamp merge by S524 doctrine (correct — the boot skeleton would otherwise preserve blanks over real cloud content) and then hard-set lastPullKeptLocal = false, so the host kept the typed value ON SCREEN while the push dedupe stayed silent and the cloud held the old value forever. The re-arm now fires from DISK EVIDENCE ONLY — ledger stamps this device minted at real edit moments, versus the cloud's own — so a boot skeleton, which has no disk stamp, can never qualify and the S524 hazard stays closed. Re-arm only: no value is merged into the applied document. THE LESSON, twice-earned: tools/sim/offlineboot.mjs asserts CLOUD WRITES, not screen state. Both earlier green-harness/red-field failures came from the test observing an outcome the product reached by a different route — the harness must measure what Mark measures.
_reassertBootLedger
_bootLedger
__arcLedgerPeek

@ S627 THE BOOT MERGE ARBITRATES TYPED VALUES (Mark, 08 Aug, THIRD field failure of the same case: type offline, close the app, reopen, old number is back). THE LESSON FIRST: three engine fixes shipped and three field failures followed, because on a real boot the LAST WORD DOES NOT BELONG TO THE ENGINE. diesel-app/js/part06d.js reads the local IDB autosave, hands it with the cloud row to _mergeCloudLocal, and applies the result. That function's own opening line states its rule - "cloud is authoritative for all fields except photo blobs" - and it returns `cloud`. It hand-rescues photo binaries, markup vectors and sketch images, and surrendered every TYPED value to the cloud. So the offline edit was stamped at its true moment (S617), persisted (S625), restored above the blank-snapshot guard (S626b) and the push re-armed - all of it now genuinely working - and then discarded HERE, one layer outside anything an engine harness observes. Every simulated device kept its own state and had no _mergeCloudLocal at all, which is exactly why they stayed green while Mark's tablet reverted. THE FIX: window.ArcBootArbitrate (published by the engine at instantiation, beside ArcSyncNow) applies the entry-time law the running app already uses everywhere else - per key, the newer ENTRY TIME wins - and the boot merge calls it just before returning. Nothing is minted; a local value survives only where this device recorded typing it LATER than the cloud's copy was typed. Cloud still wins every tie, every unstamped key and every key it holds more recently, so the S488/S601 stale-local protection is intact; structure, arrays and photos are untouched. Do NOT restore "cloud wins all typed fields" and do NOT extend this to arrays or photos - the binary-rescue passes above it are field-proven and separate. THE PROOF: tools/sim/bootmerge.mjs drives _mergeCloudLocal VERBATIM out of the shipped file (77 in, 200 out on the old host - Mark's exact revert, reproduced in code for the first time; 4/4 with the fix), and asserts BOTH negative controls - a stale local copy must still yield, and a local-only photo binary must still be rescued. Any future harness for a boot-path defect must drive the HOST function, not the engine.
ArcBootArbitrate

@ S628 THE STALENESS WARNING IS ACTUALLY ON SCREEN (Mark, after three minutes in airplane mode: "I don't think this feature exists — it only says working offline or saved locally"). He was right, and twice over. The staleness ARITHMETIC was correct from S624 and every check asserted it; what nobody asserted was whether the text reached a NODE. The host wrote it with document.getElementById('last-sync-text') — an id that has lived inside the shared header engine's SHADOW ROOT since headerEngine2 landed — so the lookup returned null and two builds of "visible staleness" wrote to nothing, and the host stylesheet's .is-stale rule could not cross the shadow boundary either. FIX, both halves: the ENGINE owns the element, so it owns the state — setCloud now accepts {stale} and toggles .cloud.is-stale, styled amber (#C98A4A light / #E0A36A dark) with an opacity override so a warning can never render fainter than the healthy text it replaces; amber never red, because the data is safe, it simply has not travelled, and reserving red for real loss keeps red meaningful. The HOST routes _renderLastSync through window.__dslHeaderCtl.setCloud instead of reaching into a shadow root it cannot see (a light-DOM fallback remains for surfaces not yet on the engine). Threshold unchanged and still read from ArcSyncCadence.desiredIntervalMs — three intended checks missed with a floor — so it follows the cadence and survives Realtime. THE STANDING LESSON: a UI claim is only true if something asserts the DOM the person would actually look at. tools/sim/stalepill.mjs mounts the REAL header, asserts the warning text and its warning styling in the shadow root, AND drives the shipped _renderLastSync verbatim to prove the host routes through the controller — the wiring half whose absence made the feature invisible while its arithmetic tested green. Fail-first: 5/7 on the shipped engine, 7/9 on the shipped host, 9/9 here.
ENGINE2_VERSION

@ S629 REALTIME — LIVE CHANGE NOTIFICATIONS (Mark asked for this repeatedly; sequenced last on purpose, behind the merge work, because a faster delivery pipe on top of a moving target hides regressions. It is also the cure for the S625 symptom "sometimes I have to refresh for the other device to catch up" — never a wrong DECISION, only a late one). THE CONTRACT, deliberately narrow and non-negotiable: the socket announces THAT the row changed and nothing else. It carries no report data, decides nothing, writes nothing. A notification runs the SAME _lookNowPull that leaving a field runs, so stamps, merge law, the collision door and offline handling are untouched, and the existing 3s throttle stops a burst of edits becoming a burst of pulls. If every socket in the fleet died the tool behaves exactly as today, only slower to notice. THE HEARTBEAT IS THE FLOOR AND IS NEVER SWITCHED OFF — a socket that drops silently must not mean a silent tool, which is the S624 gag in a new costume; isHealthy() exists so a caller may RELAX cadence while the socket is genuinely joined, never so it can stop polling. Self-echo is deliberately NOT filtered: a pull triggered by this device's own write is a no-op through contentEquals, and filtering by device id would require the write to carry one — exactly the coupling that turns a transport into a decision-maker. SERVER SIDE, and the trap that would have made this fail silently on a device: tool_data was NOT in the supabase_realtime publication at all, so the socket would have joined and sat mute forever. It is added with a PG15 COLUMN LIST — id, project_id, tool_key, instance_number, updated_at — never the whole row: the report's data jsonb runs to megabytes, and publishing it would put the report itself on a websocket for every subscriber for no benefit. The column list is also what makes this transport structurally incapable of carrying report content. Reversible with ALTER PUBLICATION supabase_realtime DROP TABLE public.tool_data. Raw Phoenix frames rather than supabase-js because the toolkit ships no bundler; the WebSocket constructor is INJECTABLE, which is how the probe drives the real code through failures. PROOF: tools/sim/realtime.mjs, 10/10, asserting the failure paths and not just the happy one — a dropped socket is noticed and reconnects with jittered backoff; a socket that OPENS BUT NEVER JOINS is treated as down (a half-open connection reports no error at all, the S624 shape); a heartbeat that cannot be sent counts as a drop; and isHealthy() is false in every one of those states, because the caller relaxes polling on that answer and a wrong "healthy" would silence the tool.
createRealtime
_rtStart
_rtStop

@ S630 ELECTRIC GETS LAYER 4, AND ONE SHARED IMPLEMENTATION (Mark, 09 Aug, reading the S624 status table: "I thought you were going to build it as part of diesel?"). He was right on both counts. S624 fixed the Android sync gag in Diesel and PORTED it to Electric BY COPYING ~90 lines; the copy was flagged at the time with its own stated cost — "two copies means two places to keep in step" — and never revisited. The cost landed exactly where predicted: Electric received layers 1-3 and silently missed layer 4, because the copy carried the staleness HELPERS while Electric had no readout for them to write to. _hbIsStale sat defined and never called for six sessions while the port was reported complete. lib/data/heartbeatLiveness.js is now the ONE implementation of the timeout, the flag watchdog, the timer-liveness check and the staleness threshold; hosts keep only their personality — their own timer variable, their own status text, and how they render the warning (Diesel through __dslHeaderCtl, Electric through __elecHeaderCtl). Storage paths and render calls stay per-host, which is the line the toolkit has held since S479. A COPIED FIX IS NOT A SHARED FIX; it is a second thing to forget. Also fixed: S628 shipped the warning as "not synced for 3m ago" in Diesel — a duration and a point in time need different words, and a warning that reads clumsily gets trusted less; both tools now render a duration. AND THE PROBE ITSELF WAS LYING: the Electric fail-first arm printed NOTHING when _renderLastSync was absent and read as a pass — the same silence that let a broken layer 4 look delivered. A probe whose subject is missing must FAIL and say why; it now reports "Electric has no _renderLastSync at all — layer 4 is absent, not merely wrong". stalepill.mjs 16/16, fail-first 13/15 on live HEAD.
createHeartbeatLiveness
_elecFmtDur
_fmtSyncDur

@ S634 THE REOPEN ARBITRATION READS ENTRY TIMES FROM THE LEDGER (Mark, 09 Aug, FOURTH field failure of the same case: type a value in airplane mode, close the app, reopen, the old number is back). S627 added the right RULE to the reopen merge — per key, the value typed later wins — and it could never take a single value on any device, because of WHERE it looked for "when was this typed". The document handed to _mergeCloudLocal at reopen is the APP's own IDB autosave (collectState output), and the string `_fts` does not occur anywhere in diesel-app/js and never has. So every local value arrived dated ZERO, the cloud's copy carried a real time, `local <= cloud` held for every field, and the revert was reliable rather than intermittent. This is the S620 fault verbatim, one layer further out: the entry times are written onto the copy that gets SENT and kept in _lastStampedLocal, which loadIDBSnapshot restores from disk at boot (S626b). _bootTs / _bootTsMap read the local time from the document first and from that ledger second. STAMP CONSERVATION IS PRESERVED (A1/A2): the ledger's time belongs to the ledger's VALUE, so it is lent ONLY while the local document still holds that exact value — a screen that has moved on is an unstamped edit, keeps losing here, and mints its true entry time at the next save. No time is invented. Do NOT drop the value-equality test to "make more edits survive": that is the borrowed-stamp fault killed at S625. Do NOT go back to reading local._fts alone. THE PROOF: tools/sim/bootstamp.mjs builds the local document the way the APP builds it (no _fts), restores the ledger through the engine's real loadIDBSnapshot, and drives the shipped _mergeCloudLocal — checks 2 and 3 FAIL on S631 and pass here; check 1 measures the shipped host source for _fts references (0) and check 5 is the no-borrowing negative control. Any future harness for this path must build its local document the way collectState does, never by hand with stamps attached.
_bootTs
_bootTsMap

@ S629 (Mark, twice: "I want this to look like the demo you showed me", "just match the demo") THE COLOUR PICKER LOOKS LIKE ITS APPROVED DEMO. The S628f build copied the demo's CSS variable NAMES (--ink, --panel, --rule) without bringing the tokens; the Hub defines none of them, so every rule fell back to a light-page default — the selection ring resolved near-black on a near-black panel and was INVISIBLE (picking a colour looked like nothing happened) and the owner initials went black on a faded tile. Tokens are now defined on #ring-host itself, both modes. THE HUB SWITCHES WITH body.dark-mode, NOT data-theme — any edit here must carry both or it breaks in one mode only. Ported from DEMO_inspector_colour_S627.html: card, uppercase section heads, 12-across grid, legend, separation note, labelled preview rows, Save/Cancel pair, and the named "taken by colleagues" list (initials on a 22px tile say a colour is spoken for but not by whom, and "whose colour is that" is the question the feature exists to answer). NEW ROW: the contractor tip, showing three trades at once — ring stays yours, body still shows priority, only the point carries the trade. The preview pin is the RENDERER'S geometry, tip zone y26-42 clipped to the 0.88 inner teardrop, same as pinsGL; if the on-canvas pin changes this changes with it, because a preview that flatters is worse than none. Mark chose preview-only (option A): real pins still show the tip ONLY under the contractor lens, unchanged.
.rc-card
.rc-h2
.rc-grid
.rc-legend
.rc-note
.rc-row
.rc-sheet
.rc-badge
.rc-pill
.rc-save
.rc-cancel
_ringPinSVG
_ringColleagues
ring-cancel

@ S629b (Mark, reviewing the ported picker) FOUR CORRECTIONS + THE CARD/LIST COLOUR SPLIT. (1) Swatch initials now come from profiles.initials — the ones typed at account setup — NOT derived from the name, and the tile holds THREE letters (MZN, LAS) not two. (2) The taken tile no longer fades as a whole (that faded the label too); it keeps its colour under a mode-aware scrim with the initials above at full contrast. (3) The preview badge is the dashboard's own .pm-avatar component, same class, so it cannot drift from what the board shows — do NOT substitute a lookalike. (4) The red-linework row is GONE by Mark's call; preview is now TWO pin rows, light sheet left and dark sheet right on each, plain then contractor-tipped. (5) RING THICKNESS DOUBLED: inner teardrop 0.88 → 0.76 in pinsGL.js AND _RING_INNER in the Hub preview. THESE TWO NUMBERS MOVE TOGETHER OR THE PREVIEW LIES. (6) The tool-pill and pm-avatar colour rules lost their .view-list scope: cards were painting saturated inline colours that never followed the theme while the list painted muted mode-aware ones, so one screen showed two colour languages. One treatment, both views, both modes. List-ONLY rules (grid columns, 30px avatars) stay scoped — those are layout, not colour.
_RING_INNER

@ S630 (Mark: "a user setting option just like any other commercial app... accessible from any tool by clicking on their name in the header... we may commercialise it one day so think ahead") THE SHARED ACCOUNT PANEL. lib/ui/accountPanel.js owns the SHELL; every tool passes its own sections and NONE of them draws headings or cards of its own — that is the S478 one-implementation rule and the whole reason the file exists. THE GROUPING IS THE COMMERCIAL BOUNDARY, not decoration: ACCOUNT = identity and how you sign in (name, email, role, initials, password, lock PIN); PREFERENCES = what affects only you (inspector colour, appearance, text size); WORKSPACE = roles, active users, project access, other people's initials — NEVER in this panel, and the shell DROPS a section that asks for that group rather than rendering it. Role and initials show read-only here and stay Admin-set. The old panel made "Update Password" the panel's PRIMARY footer button — the most prominent control on a settings screen fired a credential change; password now updates from its own section beside its own fields, and the footer is a single Done. Theme and text size were already shared toolkit-wide via localStorage ARENCON_Dark / arencon-text-size, so the panel reads and writes through host callbacks — only the host knows how to apply them to its own page. Hub is wired; FRT, Diesel and Electric wire next by passing sections, no new shell.
@ S652 — MARK AUTHORISED (14 Aug, "yes") removing .ap-card / .ap-glabel / .ap-group / .ap-item.
@ Those four styled the ORIGINAL stacked-box layout (a box per group, heading above each). The design
@ Mark signed off on 09 Aug (DEMO_account_S631b.html) is sidebar navigation instead — Profile /
@ Preferences / Security down the side, one section shown at a time — so the boxes they styled no
@ longer exist. THE RULE THEY EXPRESSED IS NOT WEAKENED: the grouping is still enforced in code and a
@ section asking for the workspace group is still DROPPED rather than rendered. Recorded here rather
@ than done quietly, same as the S631 revert.
@ The panel now OWNS EVERY STYLE IT NEEDS and hosts pass DATA, never markup — that is the fix for the
@ S630 failure, where host-supplied picker markup landed in a shadow root its page CSS could not reach.
@ Verified on Mark's screen via VERIFY_account_panel_S652.html, which mounts the REAL module and REAL
@ dialog engine so the shadow boundary is genuinely present. A markup harness is never again proof.
lib/ui/accountPanel.js
openAccountPanel
.ap-scope
.ap-idrow
.ap-seg
ap-theme
ap-ts

@ S635 THE ENTRY TIME IS PINNED WHEN THE VALUE IS SAVED (Mark, 09 Aug, iPhone: type a value in airplane mode, close the app IMMEDIATELY, reopen, the old number is back — wait ten seconds first and it holds; the Android tablet holds either way). THE MECHANISM: a typed value reached the device's own saved report ~0.7s after typing (S488 watchdog), but its ENTRY TIME was recorded on a different, slower schedule — the 5.5s cloud-push debounce, or the best-effort flush fired at visibilitychange/pagehide. Android runs that flush before freezing the page; iOS very often kills the process outright when the app is swiped from the switcher, so the value landed and its time never did, and per S634 a value with no recorded time lawfully loses to the cloud's timed copy at reopen. Two save clocks, and only the slower one carried the evidence. THE FIX IS THE SHAPE, NOT THE TIMEOUT: nothing that runs at close time can be relied on on ANY platform, so stamping now rides the SAME trigger as the value save on a SHORTER debounce (STAMP_SOON_MS 500 vs the host's 700), and by the time the value is on disk its time is too — there is nothing left for a close-time flush to rescue. stampSoon is a TRAILING debounce on purpose: a leading-edge timer would stamp the half-typed "7" of a "77" and S634's value-equality rule would then refuse to lend that time to the "77" on disk, losing the edit through the very mechanism meant to save it. Do NOT raise STAMP_SOON_MS to or above the host's value-save debounce, do NOT convert it to leading-edge, and do NOT move the trigger into either host: it lives in the shared engine because Diesel reaches its save through one delegated watchdog and Electric through ~90 hand-written debounceAutosave() call sites, so one trigger here covers both tools and every field added later (S478). NO TIME IS INVENTED — this changes only WHEN the existing stampLocal pass runs, never what it records, and it touches no network. THE PROOF: tools/sim/killswitch.mjs models a KILL — it fires no pagehide, no visibilitychange, no flush and calls no save, types into a real element and then simply stops — checks 1 and 3 FAIL on S634. Any harness that ends its offline arm with a flush or a save() is testing the case that already worked.
stampSoon
STAMP_SOON_MS
_stampSoonTimer

@ S636 ONE PHOTO SERIALISER — THE CLOUD ADDRESS WAS BEING DROPPED (Lane B, twice: every new flow-test photo saves with a blank pointer, plus a backlog of records with no address). READ LIVE FROM THE DATABASE 09 Aug: record photos 326 saved / 0 with no address; checklist photos 52 / 0; flow-test photos 33 / 31; flow-test PLD 25 / 23. Not the 39 the sweep baseline assumed — FIFTY-FOUR. The distribution IS the diagnosis: no fault in uploading, in R2 or in the sync engine could spare 378 photos and hit 54 in exactly two arrays. The cause was collectState()'s three hand-written ~20-field lists — recordPhotos named r2Key/r2Url/r2Status, the two flow lists never did, so EVERY save dropped the pointer on the way out. The bytes reached cloud storage and the photo kept displaying on the device that took it (its own copy is in memory), so this was invisible until someone opened the report on a second device and found the photo simply absent. The flow lists also omitted _annotated/_mkTs/_origBackupId/_isOrigBackup, losing annotated flow photos' stroke provenance the same way. THE FIX IS THE DUPLICATION, NOT THE TWO MISSING NAMES: adding three field names to two lists leaves list number four to be written next year by someone who forgets again. _photoOut is now the ONE serialiser and every photo array goes through it, per-array extras passed in (flow: tag — which chart/equipment slot; record: kind, date). NEVER-BAKE CONTRACT HELD (S372/S553/S560): _mkDisplay and _localSrc are derived caches and must NEVER be written out — _mkDisplay recomposites from the clean original plus mk on load, _localSrc is a live object URL meaningless on another device. Do NOT add a photo array to collectState with its own inline field list. THE PROOF: tools/sim/photoout.mjs guards the SHAPE, not the three names — one _photoOut definition, every array through it, caches excluded, per-array extras intact; checks 1 and 2 FAIL on S635. STILL OPEN AFTER THIS: the 54 existing pointerless records need repair once this write fix is field-verified (NOT before — an autosave from a device whose in-memory photo still lacks the pointer writes the blank straight back), and only then does Lane B's NO_POINTER_BASELINE drop to 0.
_photoOut

@ S638 THE PHOTO-RESCUE GATE WAS SHUTTING ON SUCCESS (Mark, 09 Aug: photo added to the 3-pt 100% row on the tablet, absent on the iPhone and the PC; read live from the database that row held ZERO photos while the 0% row held five with valid addresses — the photo never reached the cloud at all). MECHANISM: none of the targeted preserve passes ADD a photo the cloud copy lacks — they only enrich photos present on both sides — so an unpushed capture depends entirely on the S335 union to survive a pull. That union's isFresh() required the photo to still hold its bytes inline (p.d) AND to be not-yet-uploaded (r2Status !== 'uploaded'). BOTH STOP BEING TRUE THE INSTANT THE UPLOAD SUCCEEDS: the status flips to 'uploaded' and the retire pass frees the inline copy precisely because the bytes are now safe in cloud storage. The rescue window therefore closed at the moment the photo became safest, and on wifi it is a second or two wide; after it, the next heartbeat or socket pull merged the photo away and the device pushed the state without it, leaving the bytes in R2 with nothing referencing them. Silent loss of field evidence, not a display bug. THE OLD GATE WAS GUARDING SOMETHING REAL: a photo deleted on another device is ALSO uploaded-and-absent-from-cloud, and resurrecting it would undo a colleague's deletion — the code could not tell the two apart so it refused both. THE DISCRIMINATOR IS THE SNAPSHOT: __arcCloudKnewPhoto(id) answers whether the last row this device pulled contained that photo id. Never seen by the cloud ⇒ nobody can have deleted it ⇒ local capture, rescue. Seen and now absent ⇒ real removal, stays removed. NULL (no snapshot yet) ⇒ fall back to the old conservative behaviour; not knowing is NEVER a licence to resurrect. Soft-deletes stay excluded (S337) and the S354 delete arbitration still runs AFTER this pass. The id set is cached against the snapshot OBJECT's identity, not a flag, so a future eighth assignment to _lastSeenSnapshot cannot serve a stale answer. Do NOT relax the cloudKnew===false test to a truthy check, and do NOT drop the null case — a fix that passes the rescue check by weakening the delete check is worse than the bug. THE PROOF: tools/sim/photounion.mjs — check 2 (rescue) FAILS on S636 while checks 3, 4 and 5 (delete stands, unknown stays out, soft-delete stands) must pass on BOTH sides.
__arcCloudKnewPhoto
_snapPhotoIds
_snapPhotoIdsFor

@ S639 ONE PHOTO WALK, NOT NINE (the shape behind seven bugs). Every pass in _mergeCloudLocal that must visit every photo surface carried its OWN enumeration of those surfaces — nine copies. Each was an independent chance to forget one, and each new surface had to be remembered nine times. The tool's own comments record five failures of exactly this before tonight: checklist photos keyed to `checklistDetails`, a name that never existed in collectState output (silent no-op, binaries wiped on EVERY cloud apply); flowTestPhotosPld with no preserve pass at all; generalDeficiencies likewise; sketches keyed to `sketches` instead of `sketchEntries` (silent no-op again); and "fifth copy of the same body". S636 (three drifting field lists) and S638 (a rescue gate that could not tell a new photo from a deleted one) are the sixth and seventh. Every one was fixed alone; the shape was never addressed. NOW: _eachPhotoArray / _eachPhoto / _eachPhotoPair are the ONLY enumeration, and preserve, the S314 binary invariant, the S335 new-photo rescue, the S354 delete arbitration and the original-backup reconcile all walk it. _preserveRowPhotos is folded in — it was a seventh copy of the preserve body differing only in also carrying the slot tag, and that rule was the right one, so tag now travels on EVERY surface rather than flow rows alone (8 of 10 surfaces were losing it). _preservePhotoArr also fills r2Key/r2Url/r2Status independently; the shared body previously copied r2Key only when r2Url was ALSO missing, leaving an incomplete address. THE RULES: add a photo surface to _eachPhotoArray and nowhere else; NEVER give a pass its own surface list again, however small the pass; _eachPhotoPair's ensure flag creates a missing ARRAY inside an existing container but must never invent a CONTAINER (a checklist item or deficiency the cloud does not have is not the merge's business); pairing is by each surface's own identity — rows by index, checklist by id, deficiencies by contractor then index — NEVER by array position across different containers (S353, where index pairing attached one photo's identity to another's slot). THE PROOF: tools/sim/photowalk.mjs builds a report with a photo on ALL TEN surfaces and asserts preserve, rescue and delete each reach every one, then asserts in the source that the shared walk still names every surface and that NO pass outside the walkers enumerates them. On S638: 4/7. The merge fell from 903 lines to 497 with no behaviour removed.
_eachPhotoArray
_eachPhoto
_eachPhotoPair

@ S640 ONE REPAINT LIST — THE DATA ARRIVED AND THE SCREEN WAS NEVER TOLD (Mark, 10 Aug: deleting a photo on the iPhone deletes it everywhere but the tablet only shows it after a refresh; earlier the same evening a photo added on one device needed a leave-and-reenter to appear on another, and flow card titles would not turn blue until the card was re-tapped). The merge was correct the whole time — reopening the report always showed the right thing, which is precisely what "data arrived, screen did not repaint" looks like from the field, and it sent three sessions hunting the merge. THE CAUSE WAS TWO REPAINT LISTS: _dslRefreshPhotoSurfaces drew the gallery but NOT the flow-test thumbs; the cloud-apply path in _applyLoadedState drew the flow-test thumbs but NEVER the gallery. So whichever surface an inspector happened to be looking at decided whether a colleague's change appeared, and for the gallery the answer was always "not until you reopen the report". Ninth and tenth instance of the shape behind S636/S638/S639 and the five before them: a list of photo surfaces maintained by hand in more than one place. NOW: _dslRefreshPhotoSurfaces is the ONLY repaint list — checklists, deficiencies, general deficiencies (S606: its data synced but the screen never redrew it), record zones, flow thumbs, PLD flow thumbs, gallery — and _applyLoadedState calls it instead of naming surfaces itself. Add a photo surface to the tool and add its renderer HERE, once. DELIBERATELY NOT IN THE LIST: renderStdTable / renderPldTable. They rebuild the flow-test INPUT rows, and this function is called from lightbox actions (markup, rotate, delete), so including them could take the caret out from under an inspector mid-entry; the cloud apply calls them separately where a full rebuild is already happening. Do NOT add them, and do NOT give any caller its own photo-surface list again. THE PROOF: tools/sim/repaint.mjs asserts one complete list, that the input-row renderers stay out of it, and that the apply path calls it rather than naming surfaces — 3/6 on S639.
_dslRefreshPhotoSurfaces

@ S641d TYPING A COMMENT STAMPS THE ITEM (Mark, 10 Aug, three devices deadlocked: IP shows "Dd", AD and PC both show "abcttt", nobody ever wins, and it looks exactly like sync being broken). THE WRITE SIDE WORKED — the cloud held IP's "Dd" — but the item's entry time still read FIVE DAYS OLD, because the comment textarea's oninput wrote the text and never stamped _ts. Only tapping Yes/No/NA stamped (S594). So three devices holding three different comment texts all carried the SAME stale stamp, every per-item merge saw a tie, every device lawfully kept its own text, and no arbitration was possible: the 200-vs-150 stalemate, on comments. The fix is one clause: the comment oninput now also writes clState[id]._ts=Date.now(), and both checklist photo-ADD sites stamp the item the same way — a comment or a photo attach IS the inspector answering (S594 doctrine verbatim: recorded the instant, in the field, on the device in their hand). Photo DELETES deliberately do not stamp the item: they carry their own delAt and are arbitrated by S354; stamping the item on delete would let a delete outrank a colleague's newer comment. THE STALEMATE SELF-HEALS: the next keystroke in any comment box mints a real time and that text wins everywhere. Do NOT remove the stamp from the oninput to "keep saves lean", and do NOT stamp on programmatic writes (load/apply paths assign clState fields too — only HUMAN input paths stamp, or merely opening a report on a second device would outrank a real answer, the exact fault S594 exists to prevent).

@ Stale-page find-and-destroy — a pre-August Hub copy must not survive on a device (S643)
_hubCacheAudit
_hubAuditSend

@ S643 THE LEDGER MUST DESCRIBE THE DEVICE, NOT THE DECISION (Mark, 09 Aug: typed 660 into NPSH on the tablet and 88 into the PRV on the phone; both died, and the cloud row afterwards held the OLD 30 wearing a stamp minted three minutes earlier — an old value certified as the newest thing anyone had typed, outranking every real edit on every device). THE FAULT IS ORDERING, NOT STAMPING. The pull wrote `_lastStampedLocal = <the merged document>` and only THEN handed that document to the host to paint. That assignment means "this device now holds these values"; it was made before the device held anything, and unconditionally — the apply that follows is swallowed by a silent catch in THREE places (the engine's own around _onModelReplaced, the facade's _applyCloudSilent, and _applyLoadedState's terminal handler), so a paint that fails, fails invisibly and the claim stands anyway. From that moment the ledger is a lie about this device and every decision built on it is wrong the same way: the stamper diffs screen against ledger, sees 30 where the ledger says 660, concludes a person just typed 30 — so ANY keystroke anywhere on the report launders the entire unpainted screen into "newest truth"; _stampedAlready reads false so the merge defends the stale value as an unpushed edit; the recorder reports a local stamp nobody earned. Three sessions were spent hunting the stamper. The stamper was reasoning correctly from a false record. NOW: _anchorLedgerToDevice re-reads what the device ACTUALLY holds immediately after the apply and makes that the ledger, reassigning entry times by value identity under the S625 law — took the applied value ⇒ the applied document's stamp; still holding what it held ⇒ its own previous stamp, so a stale reading argues with its real age and lawfully LOSES to the newer cloud copy instead of beating it; matches neither ⇒ 0, awaiting its mint at the next save. NO TIME IS INVENTED; times are moved, exactly as at the 412 door. Wired at all THREE ledger-advance doors (pull, 412 silent merge, 412 conflict resolution) — S625's own lesson was that the pull path got the honesty revert and the 412 path did not, and that is not repeated. DELIBERATELY NOT BUILT: a "is this stale or typed?" discriminator on the stamper. That question is unanswerable from the data — a screen reading 30 looks identical whether the paint failed or an inspector deliberately typed 30 back to correct a colleague — and the guessing version eats the correction. Fix the record and the stamper needs no help; tools/sim/unpainted.mjs check 2 is the negative control that proves it. NEVER RUNS ON THE BOOT PULL (opts.allowStaleOverwrite): there the host applies on its own schedule and the model is still the default skeleton, so anchoring would record a blank as this device's state and hand the next save a fabricated entry for every real value — the S593/S597 fault. IT IS ALSO THE DETECTOR: any arbitrated value the screen did not take is named to sync_diag as apply_unpainted, which is a direct OBSERVATION of the repaint failure rather than another theory about it, and it fires whether or not an exception was thrown. THE PROOF: tools/sim/unpainted.mjs — 5/7 on live HEAD (660 destroyed everywhere, cloud holding 30 with a manufactured stamp), 7/7 on the fix, both negative controls green on both arms. It required battle_device's paintfail: every simulated device painted perfectly, which is why twenty green harnesses sat over this. Do NOT remove the anchor to "simplify the pull path", do NOT move it before model.setProject, and do NOT let it run on the boot pull.
_anchorLedgerToDevice

@ S643 THE RESTORE STOPS FAILING IN SILENCE. Every line of _applyLoadedState sits in ONE try, so the first thing that throws abandons every remaining line and the screen keeps whatever it had — with no trace but a console message on a tablet nobody has a laptop next to. Mark's two standing complaints are both this: a report that opens completely EMPTY (it threw early at boot, and re-entering "fixes" it because the retry succeeds), and performance values that never repaint when a colleague's edit arrives (it threw partway). _alsStep records how far the restore got and the terminal catch reports it to sync_diag as apply_failed with the step and the exception message; the facade's _applyCloudSilent catch reports the same event with step 'facade-apply' for throws that never reach _applyLoadedState at all (the host merge, the collect, the stringify). Two layers, two reports: there is no longer a way for a cloud update to fail to reach the screen without saying so. Do NOT remove the step markers or collapse them, and do NOT make the reporting able to throw — it must never break the load it is reporting on.
_alsStep

@ One-time iOS drawer flush — the frozen July navigation entry is evicted by one real network load (S645)
arencon-drawer-flush-v1
@ S631 CORRECTION TO THE S630 ENTRY ABOVE — THE HUB IS NOT WIRED TO THE ACCOUNT PANEL. S630 shipped the panel without anyone ever seeing it rendered; a harness proved the markup and proved nothing about the look. In the live Hub the picker inside it lost its styling entirely — the 48-swatch grid collapsed into full-width bars and the cards vanished — because the panel body lives in a shadow root and the picker's CSS lives in the PAGE stylesheet, which does not cross that boundary. The Hub was reverted to the S629b profile panel, which is styled correctly, and lib/ui/accountPanel.js remains in the repo UNUSED pending a redesign against DEMO_account_S631.html (Mark: "build it as per the colour picker demo, otherwise what's the point"). DO NOT re-wire the Hub to accountPanel.js until that demo is signed off AND every style the panel body needs is injected INTO the shadow root — page CSS will never reach it. The badge preview must show a light AND a dark surface, same as the pins and the contractor tips.

@ S633c THE SHARED HEADER — ENGINE HALF ONLY. Mark's header redesign, built and demoed across all four tools (DEMO_header_breadcrumb_strip_S634.html and predecessors). SHIPPED HERE: the account avatar + its menu (Account / Sign out) at the far right behind a divider, rendered ONLY when a host passes cfg.account; presence as an overlapping avatar stack of OTHERS in their own roster colours, moved into the START OF THE RIGHT GROUP so the buttons never shift when someone joins or leaves; the boxes stripped off presence and the IDB meter (Mark: "I never liked it but never called you out on it"); ctl.setAccount and ctl.setBreadcrumb added. THE LOGO IS NO LONGER A CONTROL — field staff mis-tapped it and were thrown out of a review mid-job; Mark has watched it happen. DO NOT make it clickable again. The way home is the Dashboard entry pinned at the top of the drawer, which falls back to homeHref when a host has no onHome — that entry is now the ONLY way home and must never be absent. The breadcrumb strip under the bar is PARKED by Mark: built, demoed, and left dormant (hidden until setBreadcrumb is called; nothing calls it). NOT SHIPPED YET, deliberately: the config half retiring Sign Out, Profile, the Hub user chip, the change-inspector chip and the text-size button. Those must NOT land until every host passes cfg.account, because the avatar menu is where Sign Out now lives and pushing the configs first would leave 17 staff with no way to sign out.
.crumbbar
.cseg
.csep
.cnow
.ditem
.mewrap
.meav
.mediv
.pstack
.pav
setAccount
setBreadcrumb
paintMe
paintCrumb

@ S643b THE ANCHOR IS FOR HOSTS WHOSE getProject() READS THE SCREEN — AND ONLY THOSE. S643 shipped the ledger anchor into the SHARED engine, which meant it reached FRT the same minute it reached Diesel. FRT is a different animal: its getProject() returns the in-memory model BY REFERENCE, so it always agrees with what was just applied — there is nothing for the anchor to detect, and FRT cannot suffer the S643 fault at all, because its save path reads the MODEL and never the screen, so a failed repaint there cannot launder a stale value into the cloud. Running the anchor anyway was not merely pointless, it was harmful: FRT's model carries photo dataUrls and thumbnails that the cloud shape strips, so the ledger became a deep clone of the WHOLE MODEL — cloned on every applying pull and persisted to IndexedDB beside the snapshot. On a photo-heavy review that is tens of megabytes per pull on a field tablet: memory spikes, slow pulls, and an IDB quota failure that would have surfaced to inspectors as "sync stopped working" with no visible cause. FRT's own converge/stalemate probes were green either way, because simulated reports carry no real photo payloads — a green harness proved nothing here and the risk had to be reasoned out from the model adapters. NOW: model.getProjectReadsScreen must be declared TRUE by the host, and only Diesel and Electric declare it (both hand the engine a fresh _collectCloudState() on every getProject call). Absent = off. Do NOT set this flag on FRT or on any tool whose getProject returns a live model reference, and do NOT "simplify" the anchor by removing the gate — the default must stay off so a new tool joining the engine is safe before anyone has thought about it.
getProjectReadsScreen
_anchorHostReadsScreen

@ S646 FRT STAMPS AT THE KEYSTROKE (Mark, 10 Aug: "will they lose data if they enter comments in Pins?"). Since S594 an entry time is minted AT THE KEYSTROKE and the save-time pass only carries stamps forward (S597 — every time that pass guessed, it guessed wrong). Diesel was taught to stamp; FRT never was: `_ts =` appeared NOWHERE in the tool, and the stamping walker descends only two levels while an FRT observation sits three deep (contractor -> deficiency -> observation), so the level inspectors actually type into carried no entry time at all. MEASURED on two real devices in separate processes (frt/tests/sim/frt_device.mjs + pincomment.mjs): before this, two people editing one pin ended PERMANENTLY SPLIT — tablet holding "A revision", phone holding "B revision", forever, no amount of syncing resolving it. That is the S641d deadlock in FRT's half of the estate. _entryStamp now stamps on the ten HUMAN input paths only (updateObservation, updateObsPriority, updateObsTrade manual-only, setObsRecommendation, toggleObsAddressed, updateDeficStatus, updateDeficPriority, setRecommendation, updateClosedNote) — every caller verified to be UI/viewer/assistant, never a load, apply or merge path, because a programmatic write that stamps would let merely opening a report outrank a real answer. An AI retag deliberately does NOT stamp (S641d's photo-delete reasoning: the tagger must not outrank a colleague's edit). Pin-level bulk actions stamp the PIN and NOT its observations, for the same reason. Server-anchored via window.ArcSyncNow (S622i) so a tablet with a fast clock cannot win every argument by being wrong about the time. WHAT THIS DOES NOT FIX, and do not let anyone believe otherwise: the later edit STILL loses, and a pin created on a second device still may not propagate. Both remaining failures are FRT's 412 door DISCARDING THE LOSING DEVICE'S WORK instead of merging it, which is not a stamping problem. frt/tests/sim/soloinputs.mjs proves the wider exposure and is the file to run first next session: a colleague who merely has the report OPEN and never edits it still overwrites the working inspector's text on its heartbeat, and two inspectors on entirely DIFFERENT pins end with one inspector's pins wholly absent. Offline capture is sound (case C green). Do NOT remove these stamps, and do NOT add stamping to any programmatic path.
_entryStamp

@ Session guard — sign-out applies to EVERY user, not only PIN holders (S648)
@ S651: idleLimitMs / deviceIsShared / noteDeviceUser removed on Mark's authorisation —
@ device tiering was retired in S650 (the app cannot tell a shared tablet from a desk).
checkInactivity
sessionStartedAt
workIsUnsynced
saveBeforeLock

@ Mandatory PIN enrolment + two-stage lock/sign-out (Mark's model, S650)
startEnrolment
submitEnrolment
paintEnrolment
pinIsWeak
hashPinStrong
verifyPinAgainst
cachePinLocally
cachedPinFor
getLockedAt

@ Boot barrier + boot anchor — relaunch can no longer launder blanks or destroy unsent work (Mark's field test 3, S673)
bootApplyComplete
_liftBootHold
_bootApplied
holdEditStamps
anchorBoot

@ Keystroke durability — a claim and its value are made permanent together (Mark's 21 Aug Rated Flow loss, S674)
onStampPersist
_persistAtStamp

@ Any edit is an edit — taps, strokes and silent arrivals are durable via the shared engine, opt-in per tool (Mark's directive, S675)
wideEditTriggers
@ S676 — FRT edit durability (Lane A, from the Lane C work order). The barrier/door
@ symbols above now also exist in FRT's facade; isDirty is the FRT door's diff gate
@ on the model and is new to the manifest.
isDirty

@ NFPA 20 acceptance extracted to the shared module — unification Phase 1 (Lane C, S676b)
PumpAcceptance
evalStdPoint
evalPldPoint
overallVerdict
gateTarget
nfpa20Gate

@ S677 — ONE project photo running order (Mark: arrows must not stop inside a pin,
@ a thread or the activity log). Removing any of these silently re-walls the viewer.
buildProjectPhotoList
openInProject
_frtLabelProjectPhotos

@ S678 — advisory WORDING is the host's, the judgement stays shared (Mark's decision,
@ 22 Aug). _dslAdvisoryText is the only place a Diesel flow-point advisory sentence
@ exists; delete it and every advisory silently stops printing on a report while the
@ pump still scores exactly the same. Pinned to a pre-move golden capture by
@ tools/sim/advisorytext.mjs.
_dslAdvisoryText

@ S679-A — lightbox unification Phase L1 (Lane A): the shared shell's per-host
@ personality. _ENG is the markup-engine indirection every engine touch runs
@ through; removing it re-hardwires the shell to one tool.
_ENG

@ S679 — unification Phase 2. One declared list of what a report IS, walked by the
@ shared engine, replacing two hand-maintained lists that nothing kept in step. Lose
@ any of these and Diesel silently stops saving or restoring whole families of a
@ report — the S496 witness-signature failure, but for every key at once.
@ (Re-landed: a concurrent lane pushed this file from a pre-S679 base and dropped
@ the block. Both lanes' entries are kept — nothing of theirs removed.)
ReportState
DieselReportManifest
dieselStateEnv
dieselCollectViaManifest
dieselApplyViaManifest

@ S680 — unified update engine. Every tool updates itself at a safe moment; the boot
@ window is what makes a fresh login apply silently instead of asking. Remove any of
@ these and a tool goes back to needing a manual hard-refresh in the field, which is
@ the state Diesel and Electric were in until now.
BOOT_APPLY_MS
_withinBootWindow
_declaredBuildChanged

@ S679-B — lightbox unification Phase L2 (Lane A): FRT's personality module and
@ the shell's host-persistence delegation. Removing any of these silently routes
@ an FRT markup save into the bake pipeline — the exact failure Rule 1 forbids.
buildFrtLightboxHooks
persistMarkup
clearMarkup
revertMarkup
photoSrcFallback

@ S680b — every tool's build stamp in a file small enough for the update engine to
@ re-read. Without these the engine cannot tell whose push it was, and a tool goes
@ back to announcing every other lane's push as its own — the pill-fatigue failure.
HUB_BUILD
ELEC_BUILD

@ S679-C — lightbox unification Phase L3: the cutover shim's contract. These are
@ the members every FRT surface and the Android back-peel call; the rescue names
@ carry the S628c/S650 protected feature into the unified viewer.
_flushMarkupForUnload
_restoreMarkupRescue
_clearMarkupRescue

@ S681 — unification Phase 3, first cut: the photo deletion model. PhotoLifecycle
@ owns whether a photograph still exists — the phantom-delete guard that stopped the
@ 7155.51 loss, delState as the authority with the legacy booleans mirrored for older
@ cached builds, and a restore recorded as a decision. Remove any of these and photos
@ start disappearing from signed reports with no error anywhere.
PhotoLifecycle
markDeleted
markLive
normalizeAll
PHANTOM_WINDOW_MS

@ S682 — unification Phase 3, second cut: when a photograph was taken. PhotoDate owns
@ the EXIF read, the id fallback that rescues photos arriving from another device with
@ their dates stripped by sync, and the NOON rule — a camera gives a calendar date with
@ no time zone, and reading it as midnight files a photo taken on the 21st under the
@ 20th for everyone west of UTC. Nothing about that failure looks wrong on screen.
PhotoDate
timestampOf
dateForNewPhoto
parseExifDate

@ S683 — unification Phase 3, third cut: which photographs are visible. PhotoInventory
@ owns the rule, and sources reach the list only through emit, so the filter cannot be
@ skipped. It fails both ways and both have happened: hard-code it live-only and
@ Recently Deleted is permanently empty while Restore can never find a photo (S367);
@ loosen the default and a soft-deleted placard appears in an issued client PDF.
PhotoInventory
isVisible

@ S684 — unification Phase 3, fourth cut: retention. The ONLY irreversible path in the
@ toolkit. expiredAmong returns the eligible set already in removal order because photos
@ come out of arrays by position — remove a low index first and every later one shifts,
@ so the next removal destroys a photograph nobody selected while the one that was meant
@ to go survives. A deleted photo with no readable timestamp is never purged, on purpose.
PhotoRetention
expiredAmong
removalOrder
isExpired

@ S684b — unification Phase 3, fifth cut: both ends of the AI photo-scan. VisionPrep
@ owns the image the service will accept (an R2-refetched blob can carry a media type
@ that 400s with no explanation; undecodable bytes are marked unreadable, never sent —
@ S502/S509c) and the JSON salvage walk (last candidate first, keyed by what the caller
@ says a real answer must contain). groupByDay in PhotoDate is the gallery's day fold.
VisionPrep
salvageJson
downscaleForVision
groupByDay

@ S685 — unification Phase 4 opens: the shared report-PDF engine. appendixEligible is
@ the mechanism deciding which KINDS of photograph may appear in a client PDF appendix
@ (the S316 scope itself stays with each tool as data — no site records, checklist or
@ deficiency photos in Diesel's). sizeChartsForPrint carries the 308-dpi print numbers
@ AND the restore closure — skip the restore and every chart sits broken after export.
ReportPdf
appendixEligible
sizeChartsForPrint

@ S685b — Phase 4 resident 3: the photo-appendix assembly, moved verbatim into the
@ engine. Its output is pinned BYTE-IDENTICAL to the pre-extraction exporter — one
@ changed byte for the same inputs is a changed client deliverable and goes red.
appendixHTML



@ U1 (S686, Mark: "complete U1") — ONE PIN-DRAG LAW. A pin coordinate is report data:
@ it is what a drawing says about where a problem is, read months later by people who
@ were never on site. Three surfaces move pins (finger on the drawing, mouse on the
@ drawing, the small map inside the pin card) and each carried its own copy of the
@ rules — three tolerance constants, two evidence logs. frt/js/viewer/pinDrag.js is now
@ the only implementation; the hosts call it and own only their gestures and paint.
@ THE DEFECT IT CLOSED: the pin-card map wrote the pin on every drag frame and captured
@ nothing, so an interrupted drag — second finger, app backgrounded for the camera,
@ editor closing — kept the position the finger happened to be over, and the next
@ automatic save made it permanent. No gesture was ever completed. The drawing viewer
@ has restored in that case since S569; this surface never did. capture/restore/preview/
@ commit/commitPreviewed are the invariants in code: intent is proven not inferred, a
@ gesture is a preview until it ends cleanly, an impossible answer is REFUSED not
@ clamped (clamping is why six pins sit at exactly 0.000/1.000 in the history).
@ Removing any of these returns a surface to writing pins by its own rules.
@ NOTE the preview clamp difference is deliberate and NOT a drift to "fix": the big
@ viewer lets a pin stick to the sheet edge during a drag, the pin-card map stops
@ following past the edge. Both END validated through the same law.
capture
restore
preview
commitPreviewed
validateLogged
_pinWriteContext

@ ────────────────────────────────────────────────────────────────────────────
@ U4 PURGE HALF (S687, Mark: "A") — FRT ON THE SHARED RETENTION LAW.
@ Destroying a photograph is the only thing this toolkit does that cannot be
@ undone. The question "may this be destroyed yet" now has ONE answer for the
@ whole toolkit (lib/data/photoRetention.js) instead of one per tool.
@
@ _PR is the accessor, and it deliberately has NO FALLBACK: no law loaded means
@ no sweep runs. A photo kept too long costs storage; a photo destroyed early
@ costs evidence that cannot be re-taken.
@
@ _makePurgedTombstone (already listed above) is the REMOVAL, and it is
@ deliberately NOT shared. FRT converts the record to a tombstone in place and
@ keeps id and R2 key; removing it erases the deletion signal and every other
@ device holding that photo re-adds it on the next sync (S43x resurrection).
@ Anyone "finishing the unification" by adopting the shared splice is causing
@ photo resurrection, and frt/tests/sim/retentionlaw.mjs check 8 fails if they
@ do. That failure is correct.
@
@ _trashDaysRemaining and _TRASH_RETENTION_DAYS are the countdown the inspector
@ reads. They must keep deriving from the same law the sweep acts on — a
@ countdown that disagrees with the sweep tells someone a photo is safe for
@ another fortnight on the morning it is destroyed.
@ ────────────────────────────────────────────────────────────────────────────
_PR
purgeExpiredPhotos
_trashDaysRemaining
_TRASH_RETENTION_DAYS

@ ────────────────────────────────────────────────────────────────────────────
@ THE AUTHORITY MARK (S692, Mark: "A" — "I need a commercial grade solution").
@ On 25 Aug 2026 three correct repairs to 7155.34 were each put back within
@ minutes by a device holding a cached copy. Nothing was broken: every guard in
@ lib/data/sync.js exists to stop a report SHRINKING, and an authorised
@ correction is byte-for-byte indistinguishable from the corruption they were
@ built to prevent. There was simply no way to TELL them.
@
@ authority_rev is a server-owned counter per report row; a Postgres trigger
@ refuses any client attempt to write it. When the cloud's mark exceeds the one
@ this device accepted, the cloud copy is taken WHOLE — no guards, no per-item
@ merge — at BOTH doors: the ordinary pull and the 412 conflict path. Removing
@ the gate at either door restores the 25 Aug loop; the conflict door is the
@ one that actually did the damage, because a device mid-save never reaches the
@ pull door at all.
@
@ _setAsideLocalCopy is what makes the takeover safe rather than a second kind
@ of data loss: the outgoing copy is written to IDB before it is replaced, so a
@ WRONG correction is always recoverable from the device that held the work.
@ Never "simplify" the takeover by dropping it.
@
@ reloadFromCloud is the escape hatch that had to exist. Until S692 the only
@ way to make a device let go of a superseded copy was clearing site data —
@ which no commercial app asks of its users and which also destroys every other
@ project cached on that machine. Mark named this directly. It must stay
@ reachable from the menu, and it must stay behind a confirm.
@ ────────────────────────────────────────────────────────────────────────────
_authSeen
_setAsideLocalCopy
reloadFromCloud
_reloadFromCloud

@ ────────────────────────────────────────────────────────────────────────────
@ S693 — THE ISSUED-REPORT LOCK (Mark's #1 after 7155.34, built S693).
@ An issued report went to a client; its content does not move. The server
@ refuses content changes with a TYPED answer (errcode PT423 -> HTTP 423,
@ trigger trg_tool_data_issued_lock), and the engine treats that refusal as a
@ STATE: unsent work is set aside, the retry machinery stands down, and the
@ host tells the person once, in plain words, where to continue. Before this,
@ a locked report was retried every 15 s forever, and on 25 Aug an issued
@ report silently accepted two weeks of fieldwork.
@
@ _issuedLocked is the engine's memory that the server said no. It gates
@ nothing destructive — it exists so the refusal is announced as a state, the
@ banner can be lifted on unlock, and diagnostics can tell a lock from a
@ network failure. It clears at the pull door when the row's status is no
@ longer 'issued' — the deliberate, recorded unlock (Revise / Revert to
@ Draft), which flips the ROW status first because the data PATCH carries no
@ status. Removing the PT423 branch in sync.js restores the silent-retry
@ defect; removing the pull-door clear leaves a device locked forever after a
@ legitimate unlock.
@ ────────────────────────────────────────────────────────────────────────────
_issuedLocked
_s693LockToldFor

@ S693 — one flow formatter: every printed flow carries thousands separators and
@ at most one decimal (the 2,353.383 gpm defect, Mark's field export). Removing
@ it puts raw float sums back on client reports.
_fmtFlow

@ S694 — pagination is resident 4 of the shared PDF engine (moved verbatim from
@ the S693-fixed host, page-identity proven in real layout). The host delegates;
@ deleting either side de-unifies the report.
paginate

@ S695 — deletion is an event, not an absence (Mark, Aug 2026): sign-row and
@ flow-photo deletes write tombstones the merge carries, so a deletion finally
@ propagates and sticks; Clean Up Empty is the person-triggered reaper for the
@ 1490.04 append-bug rows. Reverting any of these reopens resurrection.
removeSignRow
_signVisible
cleanupEmptySignRows
_ftDelete
_ftDeletePld

@ S696 — the export panel is resident 5 of the shared PDF engine. Its rules are
@ Mark-stated: the owner is ALWAYS on the distribution (S692), the vendor the
@ report is about ticks by its own evidence, the saved choice is remembered, and
@ names are matched normalised so one trailing full stop cannot print an owner
@ twice on a client report (S691). Deleting either side de-unifies the report.
exportPanel
exportPanelSelected

@ ────────────────────────────────────────────────────────────────────────────
@ S698 — A FAILED READ MUST NOT BECOME AN ADOPT (doctrine I-14).
@ These three guard the one path where a device does not know whether it is
@ holding unsent work. FRT: if the device's own saved copy cannot be READ at
@ boot after retries, cloud writes are HELD and the person is told plainly —
@ the old code treated "could not read" as "nothing there", pulled in adopt
@ mode, and replaced unsent edits it had never seen. Diesel/Electric: if the
@ boot stamp-merge throws, the device's disk copy stands and the push is held;
@ the old catch left the RAW cloud copy in `data`, so the one path where the
@ stamp law could not run was also the one path that discarded disk work.
@ Removing any of these restores an adopt-through-the-error-door.
@ ────────────────────────────────────────────────────────────────────────────
_frtBootLocalUnreadable
_s698ShowLocalUnreadable
_s698MergeHeld
