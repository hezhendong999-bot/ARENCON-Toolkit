/**
 * ARENCON FRT v2 — Photos UI
 * ═══════════════════════════
 * 
 * Renders and manages the Photos tab:
 *   - Site photos grid (unified gallery)
 *   - Photo thumbnails with checkbox, type badge, R2 icon
 *   - Lightbox with zoom (cursor-based, pinch, double-tap)
 *   - Markup lightbox (canvas-based drawing on photos)
 *   - Upload (drag & drop + camera + file picker)
 *   - Assign-to-pin from gallery/lightbox
 *   - Bulk operations (download, delete)
 * 
 * Photo zones use drag & drop as default input method.
 * Camera stays open continuously after each capture.
 */

import { Model } from '../data/model.js';

export const initPhotos = {

  render() {
    console.log('[Photos] render() — stub');
  }
};
